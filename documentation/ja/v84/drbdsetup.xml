<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE refentry PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd">
<refentry id="re-drbdsetup">
  <refentryinfo>
    <date>6 May 2011</date>

    <productname>DRBD</productname>

    <productnumber>8.4.0</productnumber>
  </refentryinfo>

  <refmeta>
    <refentrytitle>drbdsetup</refentrytitle>

    <manvolnum>8</manvolnum> <refmiscinfo class="manual">System Administration</refmiscinfo> </refmeta> <refnamediv> <refname>drbdsetup</refname> <refpurpose>DRBD を設定するツール<indexterm significance="normal">
        <primary>drbdsetup</primary>
      </indexterm></refpurpose> </refnamediv>

  <refsynopsisdiv>
    <xi:include href="drbdsetup_new-resource.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_new-minor.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_del-resource.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_del-minor.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_attach.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_connect.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_disk-options.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_net-options.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_resource-options.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_disconnect.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_detach.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_primary.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_secondary.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_down.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_verify.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_invalidate.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_invalidate-remote.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_wait-connect.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_wait-sync.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_role.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_cstate.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_dstate.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_resize.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_check-resize.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_pause-sync.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_resume-sync.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_outdate.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_show-gi.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_get-gi.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_show.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_suspend-io.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_resume-io.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_status.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_events2.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_events.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

    <xi:include href="drbdsetup_new-current-uuid.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>

  </refsynopsisdiv>

  <refsect1>
    <title>Description</title>

    <para>drbdsetup は、DRBD デバイスと下位レベルブロックデバイスを結びつける、DRBD デバイス間で下位レベルデバイス同士ミラーリングを設定する、現在実行中の DRBD デバイスの設定を検査する、などの目的で使用する。</para>
  </refsect1>

  <refsect1>
    <title>Note</title>

    <para>drbdsetup は DRBD プログラム群の中で低レベルのツールである。デバイスドライバを操作するために、drbddisk や drbd ス クリプトなどの中で使用される。</para>
  </refsect1>

  <refsect1>
    <title>Commands</title>

    <para>サブコマンドの中には、独自の引数やオプションを持つものがある。すべての値はデフォルトの単位があるが、K、 M、 G  といった通常の方法でも定義でき (K=2^10=1024、 M=1024K、 G=1024M)。</para>

    <refsect2>
      <title>Common options</title>

      <para>すべての drbdsetup サブコマンドに次のオプションを指定することができる。<variablelist>
          <varlistentry>
            <term><option>--create-device</option></term>

            <listitem>
              <para>指定した DRBD デバイスに対応するデバイスファイルがない場合、自動的に作成するするよう指定する。</para>
            </listitem>
          </varlistentry>
        </variablelist></para>
    </refsect2>

    <refsect2>
      <title>new-resource</title>

      <para>リソースは DRBD 構成の最初の対象である。リソースはいかなるボリュームやマイナーデバイスが作成されるより前に <option>new-resource</option> コマンドで作成されなければならない。コネクションは名前で参照される。</para>
    </refsect2>

    <refsect2>
      <title>new-minor</title>

      <para><emphasis>minor</emphasis> は複製されたブロックデバイスと同じ意味で使われ、ブロックデバイスとして /dev/ ディレクトリで表示されている。これは DRBD が複製するブロックデバイスへのアプリケーションのインターフェースである。drbdsetup のコマンドラインで、これらのブロックデバイスはマイナー番号によってアドレス指定される。</para>

      <para>複製されたブロックデバイスの組は 2 マシン間で異なったマイナー番号を持つ場合もあるが、共通の<emphasis>ボリューム番号</emphasis>で関連付けされている。ボリューム番号は各々の接続におけるローカルなものである。マイナー番号は一つのノードにおけるグローバルなものである。</para>
    </refsect2>

    <refsect2>
      <title>del-resource</title>

      <para>リソースのオブジェクトを破棄する。リソースに値がない場合のみ可能。</para>
    </refsect2>

    <refsect2>
      <title>del-minor</title>

      <para>マイナーはディスクが外されている場合のみ破棄できる。</para>
    </refsect2>

    <refsect2>
      <title>attach, disk-options</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>disk</secondary>
      </indexterm>

      <para>Attach は<replaceable>デバイス</replaceable>と<replaceable>下位ブロックデバイス</replaceable>を接続する。<option>-d</option> (または <option>--disk-size</option>) は下位ブロックデバイスをできるだけ使いたくない場合にのみ使用する。<option>-d</option> を使わない場合、<replaceable>デバイス</replaceable>は対向ノードに接続されるとすぐに使用できる状態になる。(<option>net</option> コマンドを参照)</para>

      <para>disk-options コマンドを使うと、接続されている間、そのマイナーのオプション変更ができる。</para>

      <variablelist>
        <varlistentry>
          <term><option>--disk-size
          <replaceable>size</replaceable></option></term>

          <listitem>
            <para>DRBD は利用できるデータ領域を自動的に決定する機能を持つ。これまで対向ノードに接続したことのないデバイスを使用する場合に、DRBD デバイスの<replaceable>サイズ</replaceable>をドライバに渡すためにこのオプションを使用する。デフォルトの単位はセクタ (1s=512バイト) である。</para>

            <para><replaceable>size</replaceable> パラメータを drbd.conf で使用する場合には、明示的に単位を追加する事が推奨される。drbdadm と drbdsetup はデフォルトの単位で、しばしば食い違いになることがある。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--on-io-error
          <replaceable>err_handler</replaceable></option></term>

          <listitem>
            <para><replaceable>下位デバイス</replaceable>のドライバがエラーを伝えた場合に DRBD はディスクを不整合とマークする。そしてヘルパープログラムを呼び出すか、デバイスを下位ストレージから切り離して以後の I/O を対向ノードに委ねる。<replaceable>err_handlers</replaceable> に指定できる値は、 <option>pass_on</option>,  <option>call-local-io-error</option>, <option>detach</option> である。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--fencing
          <replaceable>fencing_policy</replaceable></option></term>

          <listitem>
            <para><option>fencing</option> は、2つのノードがともにプライマリになること (スプリットブレイン) を防止し、どちらかを切り離す防御手段のことである。</para>

            <para>次のフェンシングポリシーを指定できる: </para>

            <variablelist>
              <varlistentry>
                <term><option>dont-care</option></term>

                <listitem>
                  <para>デフォルトの設定値で、フェンシングのためのアクションをなにも実行しない。 </para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term><option>resource-only</option></term>

                <listitem>
                  <para>ノードが切り離されたプライマリ状態になると、DRBD は他ノードを無効状態に変えようとして、fence-peer ハンドラを実行する。このハンドラには、レプリケーション用とは別のネットワーク経由で他ノードにアクセスし、 'drbdadm outdate res' を実行するという機能が期待される。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term><option>resource-and-stonith</option></term>

                <listitem>
                  <para>ノードが切り離されたプライマリ状態になると、 DRBD はすべてのディスク I/O を停止して fence-peer ハンドラを呼び出す。このハンドラには、レプリケーション用とは別のネットワーク経由で他ノードにアクセスし、 'drbdadm outdate res' を実行するという機能を想定している。ハンドラが他ノードに到達できない場合、 DRBD は STONITH 機能を使って他ノードを強制排除する。これらが完了したら、ディスク I/O を再開する。ハンドラが失敗した場合には、 <option>resume-io</option> コマンドでディスク I/O を再開できる。</para>
                </listitem>
              </varlistentry>
            </variablelist>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--disk-barrier</option></term>

          <term><option>--disk-flushes</option></term>

          <term><option>--disk-drain</option></term>

          <listitem>
            <para>DRBD は下位デバイスに対する複数のディスク書き込みの間の依存関係を指定するための 4 種類のオプションを用意している。そのうち、下位デバイスがサポートしていてユーザが無効に設定していない最初のオプションが使用される。デフォルトでは <emphasis>flush</emphasis> メソッドが使われる。</para>

	    <para>drbd-8.4.2 から <option>disk-barrier</option> は、linux-2.6.36 (及び RHEL6 の 2.6.32) 以降での正常動作が確認できないためデフォルトで無効になった。注意: 有識者による指示のもとでのみ使用すること。</para>

            <para>手法を選択するにあたっては、測定可能なパフォーマンスデータのみに頼るべきではない。下位デバイスが揮発性の書き込みキャッシュしか持たない場合 (通常のハードディスクや通常のハードディスクだけで構成される RAID など)、最初の 2 つのオプションのどちらかを使うべきである。下位デバイスにバッテリバックアップ機能付きの書き込みキャッシュがある場合には、3 番目のオプションが利用できる。4番目のオプション (すべてを無効にする場合は &quot;none&quot;) は、ほとんどの I/O スタックにおいて危険で、ディスクへの書き込み順序が入れ替わってしまう可能性がある。<option>no-disk-drain</option> を使っては <emphasis>ならない </emphasis>。</para>

            <para>残念なことに、デバイスマッパ(LVM) はバリアをサポートしていない。</para>

            <para>/proc/drbd の &quot;wo:&quot; の文字の後ろに、下位デバイスに対する現在の設定が b, f, d, n の文字で表示される。オプションは次のとおり: </para>
            <variablelist>
              <varlistentry>
                <term>barrier</term>

                <listitem>
                  <para>下位デバイスがバリア (SCSI では &quot;tagged command queuing&quot;、SATA では &quot;native com-mand queuing&quot; と呼ばれる) をサポートしている場合、このオプションを選択できる。このオプションを有効にするには <option>disk-barrier</option> オプションを <option>yes</option> にする。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term>flush</term>

                <listitem>
                  <para>下位デバイスがディスクフラッシュ (ベンダーは &quot;force unit access&quot; と呼んでいる) をサポートしている場合、このオプションを選択できる。このオプションを無効にするには <option>disk-flushes</option> を <option>no</option> に設定する。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term>drain</term>

                <listitem>
                  <para>３番目の方法は、単純に、最初の書き込みは次の書き込みリクエストを処理する前に吐き出す、方法である。8.0.9 まではこれが唯一のオプションであった。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term>none</term>

                <listitem>
                  <para><option>no-disk-drain</option> を指定すると、下位デバイスへの書き込みの依存関係を一切指示しなくなる。これはほとんどの I/O スタックにおいて <emphasis>危険</emphasis> であり、ディスクへの書き込み順序が入れ替わってしまう可能性がある。そうなると、理論的にはデータ破損の原因、または DRBD プロトコルを乱し、再接続と切断を繰り返すような状態に陥る可能性がある。<option>no-disk-drain</option> を <emphasis>使ってはならない</emphasis>。</para>
                </listitem>
              </varlistentry>
            </variablelist>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--md-flushes</option></term>

          <listitem>
            <para>メタデータデバイスへのアクセスにあたってバリアやフラッシュを使用しない。 See the notes on <option>--disk-flushes</option>.</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--max-bio-bvecs</option></term>

          <listitem>
            <para>ある特別な環境において、デバイスマッパースタックは、複数の bvec を持つ制約に違反する BIO を merge_bvec（） 関数の４番目の引数に設定し DRBD に渡す。 例えば、物理ディスク → DRBD → LVM → Xen → 誤ったパーティション(63) → DomU FS の場合です。Dom0 のカーネルログには、 &quot;bio would need to, but cannot, be split:&quot; と記録される。</para>

            <para>最も良い回避方法は、VM の内部にパーティションを適切に配置する (例えば、セクタ 1024 から開始する) ことである。これは、ストレージ領域を 480 KiB を消費する。残念ながら、ほとんどの Linux パーティションツールは、奇数 (63) でパーティションを開始する。そのため、ほとんどのディストリビューションは、仮想 Linux マシンにインストールを行うと、誤ったパーティションで終了してしまう。第 2 の回避方法は、 BIO あたりの最大 DRBD bvecs (= <option>max-bio-bvecs</option>) を 1 にすることである。しかし、パフォーマンスは低下する。</para>

            <para><option>max-bio-bvecs</option> のデフォルト値は 0 で、これはユーザに制限が無いことを意味する。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--resync-rate
          <replaceable>rate</replaceable></option></term>

          <listitem>
            <para>DRBD の上位で動作するアプリケーションの円滑な実行のために、バックグラウンドの同期作業が利用する帯域幅を制限できる。デフォルト値は 250KiB/秒、デフォルト単位は KiB/秒である。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--resync-after
          <replaceable>minor</replaceable></option></term>

          <listitem>
            <para><replaceable>minor</replaceable> に指定したマイナー番号を持つデバイスが接続されると、このデバイスの再同期を開始する。接続されるまでの間は SyncPause 状態になる。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--al-extents
          <replaceable>extents</replaceable></option></term>

          <listitem>
            <para>DRBD はホットエリアを自動的に検出する。このパラメータを指定すると、ホットエリアの大きさを制御できる。各エクステントは、下位デバイスの 4MB の領域になる。予定外の事情によってプライマリノードがクラスタから切り離されると、そのときのホットエリアのデータは、次回接続したときの再同期の対象になる。このデータ構造は、メタデータ領域に書き込まれる。したがって、ホットエリアの状態更新は、メタデータデ バイスへの書き込みを引き起こす。エクステント値を大きくすると、再同期所要時間が長くなるが、メタデータの更新頻度を減らすことができる。 <replaceable>extents</replaceable> のデフォルト値は 1237 である。(最小値:7、最大値:65534)</para>
	    <para>追加の制限と必要な準備については、次のマニュアルを参照:<citerefentry><refentrytitle>drbd.conf</refentrytitle><manvolnum>5</manvolnum></citerefentry> , <citerefentry><refentrytitle>drbdmeta</refentrytitle><manvolnum>8</manvolnum></citerefentry></para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--al-updates <group choice="req" rep="norepeat">
	     <arg choice="plain" rep="norepeat">yes</arg>
	     <arg choice="plain" rep="norepeat">no</arg>
	  </group></option></term>

	  <listitem>
	    <para>DRBD のアクティビティログ処理の書き込みによって、プライマリノードのクラッシュ後の部分的な (ビットマップに基づく) 再同期でノードを up-to-date に復帰させる事ができるようになる。<option>al-updates</option> を <option>no</option> に設定すると通常の運用パフォーマンスが向上するかもしれないが、クラッシュ時にプライマリが再接続した際にはフル同期となる。デフォルト値は <option>yes</option> である。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--c-plan-ahead
          <replaceable>plan_time</replaceable></option></term>

          <term><option>--c-fill-target
          <replaceable>fill_target</replaceable></option></term>

          <term><option>--c-delay-target
          <replaceable>delay_target</replaceable></option></term>

          <term><option>--c-max-rate
          <replaceable>max_rate</replaceable></option></term>

          <listitem>
            <para><replaceable>plan_time</replaceable> に正の値を指定すると、再同期速度を動的に調整できるようになる。これは、 <replaceable>fill_target</replaceable> に指定した一定速度で、あるいは <replaceable>delay_target</replaceable> に指定した一定の遅延でデータを送信バッファに送り込むことによって実現される。調整における最大値は <replaceable>max_rate</replaceable> で指定する。</para>

            <para><replaceable>plan_time</replaceable> パラメータで調節機能の機敏さを設定する。大きな値を設定すると、調節機能のレスポンスが低下する。この値は最低でもネットワークの RTT の 5 倍以上を指定する。通常のデータ経路では <replaceable>fill_target</replaceable> に 4k から 100k を指定するのが適切である。DRBD-Proxy を使用する場合には、代わりに <replaceable>delay_target</replaceable> を使用するのが望ましい。<replaceable>delay_target</replaceable> は <replaceable>fill_target</replaceable> が 0 の場合にのみ使用できる。初期値は RTT の 5  倍が適切である。<replaceable>Max_rate</replaceable> には DRBD 間または DRBD-Proxy 間の帯域幅あるいはディスク帯域幅を指定する。</para>

            <para><replaceable>plan_time</replaceable> のデフォルト値は 0 で、0.1 秒単位で指定する。<replaceable>Fill_target</replaceable> のデフォルト値は 0 でセクタ数を指定する。<replaceable>Delay_target</replaceable> のデフォルト値は 1 (100 ミリ秒) で 0.1 秒単位で指定する。<replaceable>Max_rate</replaceable> のデフォルト値は 10240 (100MiB/s) で、 KiB/s 単位で指定する。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--c-min-rate
          <replaceable>min_rate</replaceable></option></term>

          <listitem>
            <para>再同期の際、ディスク I/O 速度を監視すると、下位レベルデバイスが再同期していないことがわかる。下位レベルデバイスがビジーで、再同期速度が <replaceable>min_rate</replaceable> を超えている場合、再同期の処理を調整する。</para>

            <para><replaceable>min_rate</replaceable> のデフォルト値は 4M で、k 単位で指定する。調整したくない場合は 0 を設定する。常に調整する場合は 1 を設定する。</para>
          </listitem>
        </varlistentry>
        <varlistentry>
          <term><option>-t</option>,
	  <option>--disk-timeout <replaceable>disk_timeout</replaceable></option></term>
          <listitem>
      <para>DRBD デバイスがデータを格納する下位レベルデバイスが、指定した <option>disk-timeout</option> 以内で I/O リクエストを完了しない場合、DRBD はこれを障害とみなす。下位デバイスは切り離され、デバイスのディスク状態はディスクレス状態になる。DRBD が 1 台以上の対向ノードに接続したとき、失敗したリクエストはそのうち 1 台に伝えられる。</para>

      <para>このオプションは <emphasis>カーネルパニックを引き起こす可能性があり、注意が必要である</emphasis>。</para>

      <para>リクエストの「中断」あるいはディスクの強制切り離しは、完全に下位デバイスをブロックまたはハンギングして、リクエストをまったく処理せずエラーも処理しなくなる。この状況ではハードリセットとフェイルオーバ以外になす術がない。</para>

      <para>「中断」すると、基本的にローカルエラーの完了を装い、すみやかにサービスの移行を行うことで安全な切り替えを行う。それでもなお、影響を受けるノードは &quot;すぐ&quot; に再起動される必要はある。</para>
      <para>リクエストを完了することで、上位レイヤーに関連するデータページを再利用させることができる。</para>

      <para>後にローカルの下位デバイスが「復帰」すると、ディスクから元のリクエストページへの DMA のデータは、うまくいくと未使用のページへランダムなデータを送るが、多くの場合その間に関係のないデータに変形してしまい、様々なダメージの原因になる。</para>

      <para>つまり遅延した正常な完了は、特に読み込みリクエストの場合 panic() の原因になる。遅延した「エラー」完了は、その都度に通知は行うが、問題ないと考えてよい。</para>
      <para><option>disk-timeout</option> のデフォルト値は 0 であり、無限のタイムアウトを意味する。タイムアウトは 0.1 秒単位で指定する。このオプションは DRBD 8.3.12. から利用できる。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--discard-zeroes-if-aligned <group choice="req" rep="norepeat">
	     <arg choice="plain" rep="norepeat">yes</arg>
	     <arg choice="plain" rep="norepeat">no</arg>
	  </group></option></term>
          <listitem>
	    <para><option> discard-zeroes-if-aligned </option> を <option>no</option> に設定すると、それぞれのバックエンドが discard_zeroes_data = false をアナウンスする場合、DRBD は常に受信側でゼロアウトにフォールバックし、プライマリ側では discard に関して通知しない。</para><para><option> discard-zeroes-if-aligned </option> を <option>yes</option> に設定すると、 discard_zeroes_data = false を通知するバックエンドであっても DRBD は discard を使用し、 discard_zeroes_data = true を通知する。</para><para>私たちは、 discard_zeroes_data 設定を完全に無視していました。確立し、期待された動作を壊さないためのデフォルト値は <option>yes</option> です。</para><para>このオプションは 8.4.7 から有効である。追加の制限と必要な準備については、次のマニュアルを参照:<citerefentry><refentrytitle>drbd.conf</refentrytitle><manvolnum>5</manvolnum></citerefentry>.
	</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term>
            <option>--read-balancing <replaceable>method</replaceable></option>
          </term>
          <listitem>
	    <para>読み込みリクエストの負荷分散で使用できる <replaceable>methods</replaceable> は <option>prefer-local</option>, <option>prefer-remote</option>, <option>round-robin</option>, <option>least-pending</option>, <option>when-congested-remote</option>, <option>32K-striping</option>, <option>64K-striping</option>, <option>128K-striping</option>, <option>256K-striping</option>, <option>512K-striping</option>, <option>1M-striping</option> である。</para>
	      <para>デフォルトは <option>prefer-local</option> です。このオプションは 8.4.1 から有効である。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term>
            <option>--rs-discard-granularity <replaceable>bytes</replaceable></option>
          </term>
          <listitem>
	    <para><option> rs-discard-granularity </option> がゼロ以外の正の値に設定されている場合、DRBD はこのサイズで再同期操作を要求する。そのようなブロックが同期ソースノード上にゼロバイトしか含まない場合、同期ターゲットノードは、その領域に対して discard/trim/unmap コマンドを発行する。</para>
	      <para>この値は、下位ブロックデバイスの discard 粒度によって制約される。<option> rs-discard-granularity </option>が下位ブロックデバイスの discard 粒度の乗数でない場合、DRBD はそれを切り上げる。この機能は、下位ブロックデバイスが discard コマンドの後に、ゼロを読み戻す場合にのみアクティブになる。</para>
	      <para>デフォルト値は 0 である。このオプションは 8.4.7 から有効である。</para>
          </listitem>
        </varlistentry>

      </variablelist>
    </refsect2>

    <refsect2>
      <title>connect, net-options</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>net</secondary>
      </indexterm>

      <para>Connect は <replaceable>device</replaceable> を <replaceable>af:local_addr:port</replaceable> の内向きのコネクションを Listen させ、また <replaceable>af:remote_addr:port</replaceable> へ接続させる。<replaceable>port</replaceable> が省略されると、デフォルトで 7788 が使用される。<replaceable>af</replaceable> が省略されると <option>ipv4</option> が使用される。他の対応するアドレスファミリは <option>ipv6</option>, <option>ssocks</option>, <option>sdp</option> がある。 ssocks は Dolphin Interconnect Solutions の 「super sockets」、sdp は Sockets Direct Protocol(Infiniband) を指す。</para>

      <para>net-options コマンドはコネクションの確立中にオプションの変更が行える。</para>

      <variablelist>
        <varlistentry>
	  <term><option>--protocol
	  <replaceable>protocol</replaceable></option></term>

          <listitem>
            <para>DRBD の TCP/IP の通信の際に使われる <replaceable>プロトコル</replaceable> を指定する。指定できるプロトコルは A, B, C である。</para>

            <para>プロトコル A: ローカルディスクとローカル TCP 送信バッファにデータを書き込んだらディスクへの書き込みが完了したと判断する。</para>

            <para>プロトコル B: ローカルディスクとリモートバッファキャッシュにデータを書き込んだらディスクへの書き込みが完了したと判断する。</para>

            <para>プロトコル C: ローカルディスクとリモートディスクの両方にデータを書き込んだらディスクへの書き込みが完了したと判断する。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--connect-int
          <replaceable>time</replaceable></option></term>

          <listitem>
            <para>対向ノードにただちに接続できない場合、DRBD は接続を繰り返し試行する。このパラメータは試行間隔を指定する。The default value is 10. 単位は秒である。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--ping-int
          <replaceable>time</replaceable></option></term>

          <listitem>
            <para>DRBD デバイス間の TCP/IP 接続があり、対向ノードから <replaceable>time</replaceable> 秒の間に何も通信が行われなかった場合、 DRBD は死活確認のためキープアライブパケットを生成する。The default value is 10. 単位は秒である。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--timeout
          <replaceable>val</replaceable></option></term>

          <listitem>
            <para>対向ノードからの応答パケットが 1/10 の <replaceable>val</replaceable> 倍の時間以内に返ってこない場合、対向ノードが死んだと判断して TCP/IP コネクションを切断する。デフォルト値は 60 で、これは 6 秒に相当する。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--sndbuf-size
          <replaceable>size</replaceable></option></term>

          <listitem>
            <para>ソケット送信バッファは、セカンダリノードに送信するパケットを格納するために使われる。この中のパケットは、ネットワーク的にはセカンダリ側から受信確認を受け取っていない。プロトコル A を使う場合は、両ノード間の同期を高めるために、このバッファサイズを増やす必要が生じる可能性がある。しかし、プライマリノードがフェイルしたときに失うデータが増えることも考慮しておく必要がある。8.0.13 から 8.2.7 までは、手動で 0 を設定しないと自動調整にならない。デフォルト値は 0 で自動調整される。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--rcvbuf-size
          <replaceable>size</replaceable></option></term>

          <listitem>
            <para>ネットワークから受信したパケットは、最初にソケットの受信バッファに保存される。そこから DRBD に使われる。8.3.1 以前は、ソケット受信バッファのサイズはソケット送信バッファと同じサイズに固定されていた。8.3.2 からこれらを別々に設定できるようになっている。0 を指定するとカーネルが自動的に設定する。デフォルト値は 0 で自動調整される。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--ko-count
          <replaceable>count</replaceable></option></term>

          <listitem>
            <para>セカンダリノードが書き込みリクエストを <replaceable>timeout</replaceable> 内で <replaceable>count</replaceable> 回以上失敗した場合、そのセカンダリノードはクラスタから排除され、プライマリノードは StandAlone モードに遷移する。この機能を無効にするには、明示的に 0 に設定する必要がある。デフォルトはバージョン間で変更されている。8.4 は 7 がデフォルト値である。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--max-epoch-size
          <replaceable>val</replaceable></option></term>

          <listitem>
            <para>このオプションを指定すると、バリアとバリアの間の書き込みリクエスト数の最大値を制限できる。|<option>--max-buffers</option> と同じ値か、取りうる最大値を指定するのが望ましい。10 より小さい値は、パフォーマンス低下をもたらす。デフォルト値は 2048 である。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--max-buffers
          <replaceable>val</replaceable></option></term>

          <listitem>
            <para>DRBD 受信スレッドに割り当てるバッファページの最大値を指定する。<option>--max-epoch-size</option> と同じ値を指定するのが望ましい。小さい値はパフォーマンス低下をもたらす。デフォルト値は 2048 で、最小は 32 である。リニア書き込みや一方がアイドル状態での再同期の際、受け取り側の I/O バックエンドに余裕がある場合にはこの値を増やすのがよい。</para>
            <para>追加の制限と必要な準備については、次のマニュアルを参照:<citerefentry><refentrytitle>drbd.conf</refentrytitle><manvolnum>5</manvolnum></citerefentry></para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--unplug-watermark
          <replaceable>val</replaceable></option></term>

          <listitem>
	    <para>この設定は、最近の明示的にスタックプラギングを使用するカーネルには効果がない (Linux kernel 2.6.39 には移植されている)。</para>
            <para>スタンバイ(セカンダリ) ノードで書き込まれていない書き込みリクエスト数が unplug-watermark を上回ると、下位デバイスに対して書き込みリクエストを送る。ストレージによっては小さい値でも良好な結果が得られるが、多くのデバイスでは max-buffers と同じ値を指定するときに最良の結果が得られる。デフォルト値は 128 で、指定できる最小値は 16、最大値は 131072 である。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--allow-two-primaries </option></term>

          <listitem>
            <para>このオプションを指定すると、両ノードにプライマリを割り当てられる。このオプションは分散共有ファイルシステムを使うときのみ指定する。現在 DRBD がサポートするファイルシステムは OCFS2 と GFS である。これら以外のファイルシステムを使うときにこのオプションを指定すると、データの破損とノードのダウンを引き起こす。 </para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--cram-hmac-alg
          <replaceable>alg</replaceable></option></term>

          <listitem>
            <para>対向ノードの認証を行いたい場合、 +HMAC アルゴリズムを指定する。対向ノードの認証は行うべきである。チャレンジ-レスポンス方式で対向ノードを認証するのに、 HMA CFC アルゴリズムが使われる。/proc/crypto に記録されている任意のダイジェストアルゴリズムを指定できる。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--shared-secret
          <replaceable>secret</replaceable></option></term>

          <listitem>
            <para>対向ノードの認証には共有秘密鍵が使用され、64文字までで指定する。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--after-sb-0pri
          <replaceable>asb-0p-policy</replaceable></option></term>

          <listitem>
            <para>指定できるポリシーは以下の通り:</para>

            <variablelist>
              <varlistentry>
                <term><option>disconnect</option></term>

                <listitem>
                  <para>自動再同期を行わず接続を切断する。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term><option>discard-younger-primary</option></term>

                <listitem>
                  <para>スプリットブレイン発生前にプライマリであったノードからの再同期を自動的に実行する。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term><option>discard-older-primary</option></term>

                <listitem>
                  <para>スプリットブレイン発生時にプライマリになったノードからの再同期を自動的に実行する。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term><option>discard-zero-changes</option></term>

                <listitem>
                  <para>プリットブレイン発生後どちらか一方のノードに書き込みがまったく行われなかったことが明白な場合、書き込みが行われたノードから行われなかったノードに対する再同期が実行される。どちらも書き込まれなかった場合は、 DRBD はランダムな判断によって 0 ブロックの再同期を実行する。両ノードに書き込みが行われた場合、このポリシーはノードの接続を切断する。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term><option>discard-least-changes</option></term>

                <listitem>
                  <para>スプリットブレイン発生後、より多くのブロックを書き込んだノードから他方に対する再同期を実行する。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term><option>discard-node-NODENAME</option></term>

                <listitem>
                  <para>指定した名前のノードに対する再同期を実行する。</para>
                </listitem>
              </varlistentry>
            </variablelist>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--after-sb-1pri
          <replaceable>asb-1p-policy</replaceable></option></term>

          <listitem>
            <para>指定できるポリシーは以下の通り:</para>

            <variablelist>
              <varlistentry>
                <term><option>disconnect</option></term>

                <listitem>
                  <para>自動再同期を行わず接続を切断する。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term><option>consensus</option></term>

                <listitem>
                  <para><option>after-sb-0pri</option> アルゴリズムの結果が現在のセカンダリノードのデータを壊すことになる場合、セカンダリノードのデータを捨てる。そうではない場合は接続を切断する。そうではない場合は接続を切断する。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term><option>discard-secondary</option></term>

                <listitem>
                  <para>セカンダリ側のデータを捨てる。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term><option>call-pri-lost-after-sb</option></term>

                <listitem>
                  <para><option>after-sb-0pri</option> アルゴリズムの判断を常に採用する。セカンダリ側のデータが正しいと判断された場合には、現在のプライマリ側で <option>pri-lost-after-sb</option>  ハンドラが呼び出される。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term><option>violently-as0p</option></term>

                <listitem>
                  <para><option>after-sb-0pri</option> アルゴリズムの判断を常に採用する。セカンダリ側のデータが正しいと判断された場合でも、プライマリ側のデータの変更箇所を受け入れる。</para>
                </listitem>
              </varlistentry>
            </variablelist>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--after-sb-2pri
          <replaceable>asb-2p-policy</replaceable></option></term>

          <listitem>
            <para>指定できるポリシーは以下の通り:</para>

            <variablelist>
              <varlistentry>
                <term><option>disconnect</option></term>

                <listitem>
                  <para>自動再同期を行わず接続を切断する。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term><option>call-pri-lost-after-sb</option></term>

                <listitem>
                  <para><option>after-sb-0pri</option> アルゴリズムの判断を常に採用する。セカンダリ側のデータが正しいと判断された場合には、現在のプライマリ側で <option>pri-lost-after-sb</option>  ハンドラが呼び出される。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term><option>violently-as0p</option></term>

                <listitem>
                  <para><option>after-sb-0pri</option> アルゴリズムの判断を常に採用する。セカンダリ側のデータが正しいと判断された場合でも、プライマリ側のデータの変更箇所を受け入れる。</para>
                </listitem>
              </varlistentry>
            </variablelist>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--always-asbp</option></term>

          <listitem>
            <para>通常、3 番目のノードが存在しないことが現在の UUID 値から明らかな場合のみ、スプリットブレイン発生後の修復ポリシーだけが適用される。</para>

            <para>このオプションを指定すると、両ノードのデータに関連性があるとして、スプリットブレイン発生後のポリシーが適用される。UUID の分析により 3 番目のノードの存在が疑われる場合には、フル同期が行われることがある。(または、なんらかの別の原因によって間違った UUID セットで判断してしまった場合)</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--rr-conflict
          <replaceable>role-resync-conflict-policy</replaceable></option></term>

          <listitem>
            <para>このオプションは、SyncTarget ノードがすでにプライマリであり、再同期が必要であることを DRBD がメタデータから推測するときの DRBD の動作を設定する。設定できる値は: <option>disconnect</option>, <option>call-pri-lost</option>, <option>violently</option> である。<option>disconnect</option> は接続を切断する。<option>call-pri-lost</option> は <option>pri-lost</option> ハンドラを呼び出す。このハンドラは、ノードの状態をセカンダリに切り替えるか、あるいはノードをクラスタから切り離すときに呼ばれる。デフォルトは <option>disconnect</option> である。</para>

            <para><option>violently</option> を指定すると、プリマリノードを強制的に同期先 (SyncTarget) にできる。このオプションを指定すると、即座に同期元 (SyncSource) のデータに書き換えられる。このオプションは、何をしているのかを理解した上で使用するように。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--data-integrity-alg
          <replaceable>hash_alg</replaceable></option></term>

          <listitem>
            <para>ネットワーク経由で受け渡されるデータの整合性を担保するために、DRBD はハッシュ値を比較する機能を備えている。通常は、TCP/IP パケット自体のヘッダに含まれる 16 ビットチェックサムで保証される。このオプション値には、カーネルがサポートする任意のダイジェストアルゴリズムを指定できる。一般的なカーネルの場合、少なくとも <option>md5</option>, <option>sha1</option>, <option>crc32c</option> のどれかが利用できる。デフォルトでは、この機能は無効である。</para>

            <para>drbd.conf マニュアルページのデータ整合性に関する説明も参照のこと。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--no-tcp-cork</option></term>

          <listitem>
            <para>DRBD は、 TCP ソケットの TCP_CORK オプションを使って、いつ追加データを受け取るか、あるいは送信キューのデータをいつフラッシュするかのヒントを得ている。この方法が悪影響を及ぼすネットワークスタックが少なくとも存在する。このオプションを指定すると、TCP_CORK ソケットオプションの設定が無効になる。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--ping-timeout
          <replaceable>ping_timeout</replaceable></option></term>

          <listitem>
            <para>このパラメータで指定した時間内にキープアライブパケットに応答しなければならない。応答パケットが返ってこない場合、その対向ノードは死んだと判断される。デフォルト値は 5(500ms) で、100ms 単位で指定する。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--discard-my-data</option></term>

          <listitem>
            <para>スプリットブレイン状態から復旧するときに、このオプションを手作業で指定する。自動的な復旧ポリシーを設定していない場合には、DRBD は接続を拒否する。このオプションを実行すると、そのノードは接続後ただちに同期先になる。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--tentative</option></term>

          <listitem>
            <para>再同期を実行させない場合などにおいて、再同期ハンドシェーク後に DRBD 接続プロセスを終了させる。DRBD がどの再同期を実行しているかについては、カーネルログで確認することができる。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
	  <term><option>--on-congestion
	  <replaceable>congestion_policy</replaceable></option></term>

	  <term><option>--congestion-fill
	  <replaceable>fill_threshold</replaceable></option></term>

	  <term><option>--congestion-extents
	  <replaceable>active_extents_threshold</replaceable></option></term>

          <listitem>
            <para>デフォルトでは、 TCP 送信キューが一杯になると、 DRBD は書き込みをブロックする。このことは DRBD が TCP で送信できるデータ量を越えてディスクに書きこもうとするアプリケーションの処理速度が低下することを意味する。</para>

            <para>DRBD-Proxy を使っている場合、送信キューが満杯になる直前に AHEAD/BEAIND モードに移行するのが望ましい。AHEAD/BEHIND モードでは、DRBD 間の通信は切断しないが、データレプリケーションは行われなくなる。</para>

            <para>AHEAD/BEHIND モードの利点は、たとえ DRBD-Proxy のバッファがすべての書き込み要求を受け入れるのに十分でなくても、アプリケーションが遅くならないことである。欠点は、対向ノードが後れをとっているため、同期状態に戻すために再同期が必要になることである。再同期中対向ノードのデータは不整合状態のままとなる。</para>

            <para><replaceable>congestion_policy</replaceable> では <option>block</option> と <option>pull-ahead</option> が使用できる。デフォルトは <option>block</option> である。<replaceable>Fill_threshold</replaceable> は 0 から 10GiB までの値を指定できる。デフォルト値は 0 で、これはチェックが無効になることを意味する。<replaceable>Active_extents_threshold</replaceable> は、 <option>al-extents</option> と同じ制限がある。</para>

            <para>AHEAD/BEHIND は DRBD8.3.10 以降で利用できる。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--verify-alg
          <replaceable>hash-alg</replaceable></option></term>

          <listitem>
            <para><command moreinfo="none">verify</command> サブコマンドでディスク内容をオンライン照合する際、 DRBD はビット単位の比較ではなく、ブロックごとのハッシュ値を計算し、対向ノードのハッシュ値と比較する。照合に利用するハッシュアルゴリズムは、このパラメータで指定する。オプション値には、カーネルがサポートする任意のダイジェストアルゴリズムを指定できる。一般的なカーネルの場合、少なくとも <option>md5</option>, <option>sha1</option>, <option>crc32c</option> のどれかが利用できる。デフォルトでは、この機能は無効である。オンライン照合を有効にするには、このパラメータを明示的に設定する必要がある。 </para>

            <para>drbd.conf マニュアルページのデータ整合性に関する説明も参照のこと。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--csums-alg
          <replaceable>hash-alg</replaceable></option></term>

          <listitem>
            <para>csums-alg が指定されていない場合、再同期プロセスはすべてのマークされたデータブロックをコピー元からコピー先に転送するこのオプションを指定すると、マークされたデータブロックのハッシュ値を最初に送り、ハッシュ値が一致しないブロックについてのみデータを転送する。</para>

            <para>帯域幅が小さい回線を使うとき、このオプションは有用である。クラッシュしたプライマリノードが復帰したとき、アクティビティログに記録されたすべてのブロックが再同期の対象となる。しかし大部分のブロックは同期が取れている。このため、 <option>csums-alg</option> を指定することによって、 CPU  の使用量と引き換えに必要な転送量を減らせる。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--use-rle</option></term>

          <listitem>
            <para>再同期開始時のハンドシェークの過程で各ノードのビットマップが交換され、ビット単位の OR 計算が行われる。これによって、どのブロックがダーティ (不一致) であるかについて、それぞれのノードは共通の認識を持つ。大容量デバイスではビットマップも大きくなり、帯域幅が小さいネットワークではその交換に時間を要する。</para>

            <para>典型的なビットマップは、すべてがセットされていない (クリーン) あるいはセットされている (ダーティ) いくつかのエリアに分かれている。このため、単純ではあるがランレングス符号化を採用することにより、ビットマップ交換のためのネットワークトラフィックを顕著に減らすことができる。</para>

            <para>過去のバージョンとの互換性のため、また高速ネットワークでは転送時間改善効果が少なく CPU 使用量が増えるため、デフォルトではこの機能は無効である。</para>

            <para>Introduced in 8.3.2.</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--socket-check-timeout</option></term>

          <listitem>
            <para>DRBD-Proxy を使っていて大量のバッファを確保する必要がある環境では ping-timeout に非現実的な大きな値を指定しなければならないことがある。TCP コネクションが開始したときの安定するのを待つ局面でも、 DRBD はデフォルトで ping-timeout を使ってしまう。DRBD-Proxy は通常、同じデータセンターに配置されているため、長い待機時間は DRBD の接続プロセスを妨げる可能性がある。</para>
	    <para>このような場合、<option>socket-check-timeout</option> に DRBD と DRBD-Proxy 間の round trip time(RTT) を設定するとよい。たいていの場合 1 である。</para>
	    <para>デフォルトの単位は 10 分の 1 秒である。デフォルト値は 0 で socket-check-timeout 値の代わりに <option>ping-timeout</option> 値を使用する。8.4.5 から導入された。</para>
          </listitem>

        </varlistentry>
      </variablelist>
    </refsect2>

    <refsect2>
      <title>resource-options</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>resource-options</secondary>
      </indexterm>

      <para>Changes the options of the resource at runtime.</para>

      <variablelist>
        <varlistentry>
          <term><option>--cpu-mask
          <replaceable>cpu-mask</replaceable></option></term>

          <listitem>
            <para>DRBD のカーネルスレッドに CPU アフィニティマスクを設定する。<replaceable>cpu-mask</replaceable> のデフォルト値は 0 で、 DRBD のカーネルスレッドがマシン全体の CPU にまたがって動作することを意味している。この値は 16 進表現で指定する必要がある。値が大きすぎると切り捨てられる。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term><option>--on-no-data-accessible
          <replaceable>ond-policy</replaceable></option></term>

          <listitem>
            <para>この設定は、ディスクレスモードが発生した際の操作を指定する。設定できるポリシーは <option>io-error</option> と <option>suspend-io</option> である。</para>

            <para><replaceable>ond-policy</replaceable> に <option>suspend-io</option> を設定すると、最後に接続していたデータストレージから、もしくは、 <command moreinfo="none">drbdadm resume-io <replaceable>res</replaceable></command> コマンドにより、 I/O を再開することができる。後者はもちろん I/O エラーを発生させる。</para>

            <para>デフォルトは <option>io-error</option> である。この設定は、 DRBD 8.3.9 から有効である。</para>
          </listitem>
        </varlistentry>
      </variablelist>
    </refsect2>

    <refsect2>
      <title>primary</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>primary</secondary>
      </indexterm>

      <para><replaceable>デバイス</replaceable>をプライマリロールに切り替える。するとアプリケーション (この場合はファイルシステムなど) は<replaceable>デバイス</replaceable>を読み書きモードでオープンできるようになる。プライマリロールの<replaceable>デバイス</replaceable>に書き込まれたデータはセカンダリロールのデバイスにミラーされる。</para>

      <para>通常の実行状態では、 DRBD デバイスペアの両方を同時にプライマリに切り替えることはできない。<option>--allow-two-primaries</option> オプションを使用すると、DRBD の既定の仕様を変更して両ノードをプライマリにできる。</para>

      <variablelist>
        <varlistentry>
          <term><option>--overwrite-data-of-peer</option></term>

          <listitem>
            <para>--force のエイリアス。</para>
          </listitem>
        </varlistentry>
      </variablelist>

      <variablelist>
        <varlistentry>
          <term><option>--force</option></term>

          <listitem>
            <para>ローカルディスクの複製物が最新 (UpToDate) でない場合は、プライマリになれない。つまり、データが一致しなかったり、古かったりする場合である。このオプションを指定すると、状況に関係なくプライマリに切り替えられる。このオプションは、何をしているのかを理解した上で使用するように。</para>
          </listitem>
        </varlistentry>
      </variablelist>
    </refsect2>

    <refsect2>
      <title>secondary</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>secondary</secondary>
      </indexterm>

      <para><replaceable>デバイス</replaceable>をセカンダリロールに切り替える。何らかのアプリケーション (またはファイルシステム) が書き込みモードでデバイスをオープンしている間は、この操作は失敗する。</para>

      <para>DRBD デバイスペアの両方がともにセカンダリ状態になることは問題なく可能である。</para>
    </refsect2>

    <refsect2>
      <title>verify</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>verify</secondary>
      </indexterm>

      <para>オンライン状態でデバイスを照合する。オンライン照合は、ローカルノードの全ブロックを対向ノードの対応するブロックと比較することである。照合の進行状況は、 <filename moreinfo="none">/proc/drbd</filename> を通じて確認できる。ローカルディスクと対向ホストで内容が異なるブロックは、 DRBD のディスク上ビットマップに不整合としてマークされる。これらは自動的な再同期が行われない。再同期を行うには、いったんリソースの接続を切って再接続すればよい。 </para>

      <para>すでにオンライン照合が進行中の場合、 (当該ノードが照合を行い) このコマンドは成功する。この場合、 start-sector (以下参照) は無視され、 stop-sector (以下参照) が採用される。これは照合を終了するため、あるいは現在進行中の照合の範囲を 更新/ 短縮/ 拡張 するために使用される。</para>

      <para>このコマンドは<replaceable>デバイス</replaceable>が接続されているデバイスペアのうちの一つでない場合は失敗する。</para>

      <para>drbd.conf マニュアルページのデータ整合性に関する説明も参照のこと。</para>

      <variablelist>
        <varlistentry>
          <term><option>--start
          <replaceable>start-sector</replaceable></option></term>

          <listitem>
            <para>バージョン 8.3.2 以降でこのオプションが利用できる。接続が切れて復旧したとき、オンライン照合は直前の最後の位置から再開する。このオプションを指定すると、任意の位置から照合を開始できる。以前に stop-sector に達した事があり、明示的に start-sector を指定しない場合には、照合は以前の停止セクタから再開される。</para>

            <para>デフォルトの単位はセクタである。単位を明示的に指定することもできる。<option>開始セクタ</option>は 8 セクタの倍数 (4kB) に切り捨てられる。</para>
          </listitem>
        </varlistentry>
        <varlistentry>
          <term><option>-S</option>,
	  <option>--stop <replaceable>stop-sector</replaceable></option></term>
          <listitem>
            <para>バージョン 8.3.14 以降では、オンライン照合はデバイスの最後に達する前に終了することができる。</para>
            <para>デフォルトの単位はセクタである。単位を明示的に指定することもできる。照合が進行中に追加の drbdsetup verify コマンドを同じノードで実行することで、 <option>stop-sector</option> を更新する事ができる。これは照合を終了するため、あるいは現在進行中の照合の範囲を 更新/ 短縮/ 拡張 するために使用される。</para>
          </listitem>
        </varlistentry>
      </variablelist>
    </refsect2>

    <refsect2>
      <title>invalidate</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>invalidate</secondary>
      </indexterm>
      <para>接続された DRBD デバイスペアのローカル側を SyncTarget 状態に切り替える。これにより、対向ホスト側のすべてのデータブロックがローカルディスクにコピーされる。</para>
      <para>デバイスが対向ホスト側のデバイスと接続していなかったり、切断されたセカンダリの場合、コマンドは失敗する。</para>
    </refsect2>

    <refsect2>
      <title>invalidate-remote</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>invalidate-remote</secondary>
      </indexterm>
      <para>接続された DRBD デバイスペアのローカルデバイス側を SyncSource 状態に切り替える。これにより、ローカルディスクのすべてのデータブロックが対向ペアにコピーされる。</para>
      <para>切断されたプライマリデバイス側では、すべてのビットが非同期のビットマップに設定される。これによってオンディスクのアクティビティログの更新が一時停止する。アクティビティログの更新が再開すると、必要に応じて自動的再開される。</para>
    </refsect2>

    <refsect2>
      <title>wait-connect</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>wait-connect</secondary>
      </indexterm>

      <para><replaceable>デバイス</replaceable>が対向デバイスと通信可能になるまで待機する。</para>

      <variablelist>
        <varlistentry>
          <term><option>--wfc-timeout
          <replaceable>wfc_timeout</replaceable></option></term>

          <term><option>--degr-wfc-timeout
          <replaceable>degr_wfc_timeout</replaceable></option></term>

          <term><option>--outdated-wfc-timeout
          <replaceable>outdated_wfc_timeout</replaceable></option></term>

          <term><option>--wait-after-sb</option></term>

          <listitem>
            <para><replaceable>デバイス</replaceable>が <replaceable>timeout</replaceable> 秒以内に対向ペアと通信できないと、このコマンドは失敗する。ノードが再起動する前に対向ノードが動作していた場合、 <replaceable>wfc_timeout</replaceable> パラメータの値が使われる。ノードが再起動する前に対向ノードがダウンしていた場合には、 <replaceable>degr_wfc_timeout</replaceable> パラメータの値が使われる。ノードが再起動する前に対向ノードが無効にされていた場合には <replaceable>outdated_wfc_timeout</replaceable> パラメータが使われる。これらすべてのタイムアウト値のデフォルトは 0 で、接続されるまで永久に待機することを表す。単位は秒である。スプリットブレイン状態が起きたためにデバイスが接続された後 StandaAlone 状態になってしまった場合、このコマンドは失敗する。<option>--wait-after-sb</option> を指定すると、デフォルトの動作を変更できる。</para>
          </listitem>
        </varlistentry>
      </variablelist>
    </refsect2>

    <refsect2>
      <title>wait-sync</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>wait-sync</secondary>
      </indexterm>

      <para><replaceable>デバイス</replaceable>の同期が終わるまで待機する。このオプションは <replaceable>wait-connect</replaceable> コマンドと同じである。</para>
    </refsect2>

    <refsect2>
      <title>disconnect</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>disconnect</secondary>
      </indexterm>

      <para><option>net</option> コマンドで設定した<replaceable>デバイス</replaceable>の情報を削除する。その結果、<replaceable>デバイス</replaceable>は切断状態になり、その後はネットワーク経由の情報を受け取らない。</para>
    </refsect2>

    <refsect2>
      <title>detach</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>detach</secondary>
      </indexterm>
      <para><option>disk</option> コマンドで設定したデバイスの情報を消去する。その結果、<replaceable>デバイス</replaceable>は下位ストレージデバイスから切り離される。<variablelist>
        <varlistentry>
          <term><option>-f</option>,
	  <option>--force</option></term>
          <listitem>
            <para>通常、detach は、ディスク状態がディスクレスになってから戻ってくる。そのため、フリーズしている下位ブロックデバイスの切り離しは終了しない。</para>
	    <para>一方で、強制切り離しは即座に返答がくる。これでフリーズした下位ブロックデバイスを切り離すことができる。ディスクは全 I/O リクエストが下位ブロックデバイスで完了するまで failed としてマークされることに注意。</para>
          </listitem>
        </varlistentry>
      </variablelist>
      </para>
    </refsect2>

    <refsect2>
      <title>down</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>down</secondary>
      </indexterm>

      <para><replaceable>デバイス</replaceable>の全ての設定情報を消去する。また、未設定の状態に戻す。</para>
    </refsect2>

    <refsect2>
      <title>role</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>role</secondary>
      </indexterm>

      <para><replaceable>デバイス</replaceable>とその対向デバイスの現在のロール (役割) を表示する。表示形式は <replaceable>ローカ ル</replaceable>/<replaceable>他ノード</replaceable>である。</para>
    </refsect2>

    <refsect2>
      <title>state</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>state</secondary>
      </indexterm>

      <para>&quot;role&quot; の別名である。</para>
    </refsect2>

    <refsect2>
      <title>cstate</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>cstate</secondary>
      </indexterm>

      <para>現在の<replaceable>デバイス</replaceable>の接続状態を表示する。</para>
    </refsect2>

    <refsect2>
      <title>dstate</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>dstate</secondary>
      </indexterm>

      <para>    現在の下位デバイスの状態を表示する。表示形式は<replaceable>ローカル</replaceable>/<replaceable>他ノード</replaceable>である。 
</para>
    </refsect2>

    <refsect2>
      <title>resize</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>resize</secondary>
      </indexterm>

      <para>下位<replaceable>デバイス</replaceable>のサイズを再検査する。実際にオンライン拡張するには、両デバイスで下位デバイスを拡張してから、そのうちの1 台で <option>resize</option> コマンドを実行する。</para>

      <para><option>--size</option> オプションは DRBD デバイスで使用できるサイズのオンライン縮小に使用される。ファイルシステムがこの操作によって破損しないことはユーザ責任において確認する。</para>

      <para><option>--assume-peer-has-space</option> オプションは、現在対向ノードに接続されていないデバイスのサイズ変更ができる。対向ノードのディスクサイズを同様に変更しないと、以降接続が失敗するので注意すること。</para>

      <para><option>--assume-clean</option> オプションを使用すると DRBD は新しいストレージの再同期をスキップする。新しいストレージが、別の方法で初期化されていることが確認できている場合のみ、これを設定する。</para>

      <para><option>--al-stripes</option> と <option>--al-stripe-size-kB</option> オプションはオンラインでアクティビティログのレイアウトを変更する。内部メタデータの場合には、同時に、下位デバイスのユーザーに見えるサイズ(<option>--size</option> を使用して)の縮小、または拡大が必要である。</para>
    </refsect2>

    <refsect2>
      <title>check-resize</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>check-resize</secondary>
      </indexterm>

      <para>DRBD が下位デバイスのオフラインリサイズを検知できるようにするため、このコマンドは下位デバイスの現在のサイズを記録するために使用される。サイズは、/var/lib/drbd/ 下に drbd-minor-??.lkbd という名前で記録される。</para>

      <para>このコマンドは <command moreinfo="none">drbdsetup <replaceable>device</replaceable> resize</command> が実行された後で <command moreinfo="none">drbdadm resize <replaceable>res</replaceable></command> からコールされる。</para>
    </refsect2>

    <refsect2>
      <title>pause-sync</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>pause-sync</secondary>
      </indexterm>

      <para>ローカルメタデータの一時停止フラグをセットして、進行中の再同期を一時停止する。再開させるには、ローカルと他ノードの両方の一時停止フラグをクリアする必要がある。下位レベルデバイスの RAID を再構成しているときなど、 DRBD の再同期を一時停止するのが望ましい場合がある。</para>
    </refsect2>

    <refsect2>
      <title>resume-sync</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>resume-sync</secondary>
      </indexterm>

      <para>自機の一時停止フラグをクリアする。</para>
    </refsect2>

    <refsect2>
      <title>outdate</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>outdate</secondary>
      </indexterm>

      <para>自ノードの下位レベルデバイスの内容が「無効」であるとマークする。無効ノードはプライマリになれない。このコマンドは通常 <option>fencing</option> や対向ノードの <option>fence-peer</option> ハンドラと組み合わせて使用される。</para>
    </refsect2>

    <refsect2>
      <title>show-gi</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>show-gi</secondary>
      </indexterm>

      <para>デバイスのデータ世代識別子 (data generation identifiers) の内容を説明テキストとともに表示する。</para>
    </refsect2>

    <refsect2>
      <title>get-gi</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>get-gi</secondary>
      </indexterm>

      <para>デバイスのデータ世代識別子 (data generation identifiers) の内容を表示する。</para>
    </refsect2>

    <refsect2>
      <title>show</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>show</secondary>
      </indexterm>

		<para>リソースの全ての有効な設定情報を表示する。利用可能なオプション: <variablelist>
				<varlistentry>
					<term><option>--show-defaults</option></term>

					<listitem>
						<para>すべての設定パラメータを表示する。設定ファイルに未設定のパラメータも初期値の設定で表示する。このオプションをつけないと、初期値のパラメータは確認できない。</para>
					</listitem>
				</varlistentry>
			</variablelist>
		</para>
    </refsect2>

    <refsect2>
      <title>suspend-io</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>suspend-io</secondary>
      </indexterm>

      <para>このコマンドの明確な使用法はないが、コマンドセットの完全性のために用意されている。</para>
    </refsect2>

    <refsect2>
      <title>resume-io</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>resume-io</secondary>
      </indexterm>

      <para><option>fencing</option> ポリシーが resource-and-stonith に設定されており、fence-peer ハンドラの実行に失敗した場合、このコマンドによってフリーズされた I/O 操作を再開できる。</para>
    </refsect2>

    <refsect2>
      <title>status</title>
	  <indexterm significance="normal">
	    <primary>drbdsetup</primary>
	    <secondary>status</secondary>
	  </indexterm>

	  <para>リソースの全ての有効な設定情報を表示する。出力は、構成されたリソースごとに 1 つの段落で構成される。各段落は、リソースごとに 1 行、各デバイスに 1 行、接続ごとに 1 行が続きます。デバイスと接続行はインテンドされます。各接続行は、対向ノードのデバイスごとに 1 行、これらは接続行に対してインテンドされます。</para>

	  <para>長い行は端末の幅で折り返され、どの行に属しているかを示すためにインデントされる。利用可能なオプション: <variablelist>
	      <varlistentry>
		<term><option>--verbose</option></term>

		<listitem>
		  <para>冗長か無関係であっても、より多くの情報を出力に含める。</para>
		</listitem>
	      </varlistentry>

	      <varlistentry>
		<term><option>--statistics</option></term>

		<listitem>
		  <para>出力にデータ転送の統計情報を含める。</para>
		</listitem>
	      </varlistentry>

	      <varlistentry>
		<term>
		  <option>--color=<group choice="req" rep="norepeat">
		      <arg choice="plain" rep="norepeat">always</arg>
		      <arg choice="plain" rep="norepeat">auto</arg>
		      <arg choice="plain" rep="norepeat">never</arg>
		    </group>
		  </option>
		</term>

		<listitem>
		  <para>出力をカラー化する。 <option>--color=auto</option>, <option>drbdsetup</option> は標準出力が端末に接続されている場合にのみカラーコードを出力する。</para>
		</listitem>
	      </varlistentry>
	    </variablelist>
	  </para>

	  <para>たとえば、1 つの接続と1 つのボリュームのみを持つリソースの出力は、次のようになる。<programlisting format="linespecific">
fs-backoffice role:Primary
  disk:UpToDate
  peer role:Secondary
    replication:Established peer-disk:UpToDate
	      </programlisting>
	    </para>

	    <para><option>--verbose</option> と <option>--statistics</option> を使用すると, 同じリソースは次のように出力される:<programlisting format="linespecific">
fs-data role:Primary suspended:no
    write-ordering:drain
  volume:0 minor:1 disk:UpToDate
      size:10616472 read:134465 written:144800 al-writes:18 bm-writes:0
      upper-pending:0 lower-pending:0 al-suspended:no blocked:no
  peer connection:Connected role:Secondary congested:no
    volume:0 replication:Established peer-disk:UpToDate resync-suspended:no
        received:122596 sent:22204 out-of-sync:0 pending:0 unacked:0
	      </programlisting>
	    </para>
    </refsect2>

    <refsect2>
      <title>events2</title>
      <indexterm significance="normal">
	<primary>drbdsetup</primary>
	<secondary>events2</secondary>
      </indexterm>

	  <para>設定されたすべての DRBD オブジェクトの現在の状態、状態へのすべての変更を表示する。</para>

	  <para>出力形式は、機械だけでなく人も読める形式である。各行はイベント番号で始まり、イベントが次の行に続く場合はアスタリスクが続く。各行の 2 番目の単語は、イベントの種類を示す: 既存オブジェクトの <option>exists</option>; オブジェクトが作成、破棄、変更された場合 <option>create</option>, <option>destroy</option>, <option>change</option>; イベントハンドラが呼び出された、戻った場合 <option>call</option>,  <option>response</option> である。第 3 の単語は、イベントが適用されるオブジェクトを示す: <option>resource</option>, <option>device</option>, <option>connection</option>, <option>peer-device</option>, <option>helper</option>, 現在の状態が完全にダンプされたことを示すための dash (<option>-</option>)</para>

	  <para>残りの単語はオブジェクトを識別し、オブジェクトが入っている状態を記述する。利用可能なオプション: <variablelist>
	      <varlistentry>
		<term><option>--now</option></term>

		<listitem>
		  <para>現在の状態を報告した後に終了する。デフォルトでは、継続的に監視し、状態の変化を報告する。</para>
		</listitem>
	      </varlistentry>

	      <varlistentry>
		<term><option>--statistics</option></term>

		<listitem>
		  <para>統計情報を出力に含める。</para>
		</listitem>
	      </varlistentry>
	    </variablelist>
	  </para>
    </refsect2>

    <refsect2>
      <title>events</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>events</secondary>
      </indexterm>

      <para>推奨しない。可能であれば、events2 サブコマンドに変更する。</para>
      <para>DRBD のすべての状態変化とヘルパープログラムの呼び出し経緯を表示する。このコマンドは、DRBD の状態変化を他のプログラムにパイプで渡したいときに利用できる。<variablelist>
          <varlistentry>
            <term><option>--all-devices</option></term>

            <listitem>
              <para>すべての DRBD デバイスの状態を表示する。</para>
            </listitem>
          </varlistentry>

          <varlistentry>
            <term><option>--unfiltered</option></term>

            <listitem>
              <para>このオプションはデバッグ用である。すべてのネットリンクレイやの受信メッセージの内容を表示する。</para>
            </listitem>
          </varlistentry>
        </variablelist></para>
    </refsect2>

    <refsect2>
      <title>new-current-uuid</title>

      <indexterm significance="normal">
        <primary>drbdsetup</primary>

        <secondary>new-current-uuid</secondary>
      </indexterm>

      <para>現在 UUID を生成して、他のすべての UUID をローテートする。このコマンドには 2 通りの利用シーンが考えられる。まず、初期同期を省略する際に利用する。また、 1 台で設定を行った後遠隔サイトに運搬することにより帯域幅を節約するためにも利用できる。</para>

      <para>利用可能なオプション: <variablelist>
          <varlistentry>
            <term><option>--clear-bitmap</option></term>

            <listitem>
              <para>現在 UUID を生成すると同時に同期ビットマップをクリアする。</para>
            </listitem>
          </varlistentry>
        </variablelist></para>

      <para>このオプションは、ゼロから新規に構築する際に初期同期を省略するために使用する。この方法は、作ったばかりのメタデータに対して効果がある。必要なステップは以下のとおり:<orderedlist continuation="restarts" inheritnum="ignore" numeration="arabic">
          <listitem>
            <simpara>両ノードでメタデータを初期化してデバイスを設定する。</simpara>

            <simpara><command moreinfo="none">drbdadm -- --force create-md
            <replaceable>res</replaceable></command></simpara>
          </listitem>

          <listitem>
            <simpara>それぞれのサイズを知るために初期ハンドシェークが必要となる。</simpara>

            <simpara><command moreinfo="none">drbdadm up
            <replaceable>res</replaceable></command></simpara>
          </listitem>

          <listitem>
            <simpara>Secondary/Secondary Inconsistent/Inconsistent の状態で接続される。新しい現在 UUID を作りダーティなビットマップをクリアする。</simpara>

            <simpara><command moreinfo="none">drbdadm new-current-uuid --clear-bitmap
            <replaceable>res</replaceable></command></simpara>
          </listitem>

          <listitem>
            <simpara>Secondary/Secondary UpToDate/UpToDate の状態で接続される。片方をプライマリに切り替えてファイルシステムを作成する。</simpara>

            <simpara><command moreinfo="none">drbdadm primary
            <replaceable>res</replaceable></command></simpara>

            <simpara><command moreinfo="none">mkfs -t <replaceable>fs-type</replaceable> $(drbdadm
            sh-dev <replaceable>res</replaceable>)</command></simpara>
          </listitem>
        </orderedlist></para>

      <para>この方法の明らかな欠点は、別の方法でディスク内容を同一にしない限り、複製物全体に古いゴミのデータが残ることである。このため、オンライン照合は多数の非同期ブロックを見出してしまう。</para>

      <para><emphasis>この方法は、すでにデータのあるディスクに適用してはならない！</emphasis> 最初は動作しているように見えても、ノードの切り替えを行うとデータが壊れてしまい二度と使えなくなる。そのため、<emphasis>mkfs を省略してはならない</emphasis>　(またはそれに相当する操作)。</para>

      <para>このコマンドは、最初のノードを本稼働させた後でディスク自体を 2 番目のノードに移すという手法により、クラスタの初期化時間を短縮する目的にも適用できる。この方法は、切断されたデバイスに対してのみ有効である。デバイスのロールはプライマリでもセカンダリでもよい。</para>

      <para>必要なステップは以下のとおり:<orderedlist continuation="restarts" inheritnum="ignore" numeration="arabic">
          <listitem>
            <simpara><command moreinfo="none">drbdsetup new-current-uuid --clear-bitmap <replaceable>minor</replaceable>
            </command></simpara>
          </listitem>

          <listitem>
            <simpara>現在のアクティブなサーバのディスクをコピーする。たとえば RAID1 コントローラ配下のディスクを 1 本抜く、 dd でコピーするなど。実際のデータ領域とメタデータの両方をコピーする必要がある。</simpara>
          </listitem>

          <listitem>
            <simpara><command moreinfo="none">drbdsetup new-current-uuid <replaceable>minor</replaceable>
            </command></simpara>
          </listitem>
        </orderedlist>このディスクをセカンダリのノードに挿入してクラスタに参加させる。ステップ 1 での <command moreinfo="none">drbdsetup</command> の実行以降に変更した箇所の再同期が行われる。</para>
    </refsect2>
  </refsect1>

  <refsect1>
    <title>Examples</title>

    <para>詳しくは DRBD ユーザーズガイド <ulink url="http://www.drbd.org/users-guide/"><citetitle>DRBD User's Guide</citetitle></ulink> を参照。</para>

  </refsect1>

  <refsect1>
    <title>Version</title>

    <simpara>このドキュメントは DRBD バージョン 8.3.2 向けに改訂されている。</simpara>
  </refsect1>

  <refsect1>
    <title>Author</title>

    <simpara>Written by Philipp Reisner <email>philipp.reisner@linbit.com</email> and Lars
    Ellenberg <email>lars.ellenberg@linbit.com</email></simpara>
  </refsect1>

  <refsect1>
    <title>Reporting Bugs</title>

    <simpara>Report bugs to <email>drbd-user@lists.linbit.com</email>.</simpara>
  </refsect1>

  <refsect1>
    <title>Copyright</title>

    <simpara>Copyright 2001-2008 LINBIT Information Technologies, Philipp Reisner, Lars Ellenberg.
    This is free software; see the source for copying conditions. There is NO warranty; not even
    for MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.</simpara>
  </refsect1>

  <refsect1>
    <title>See Also</title>

    <para><citerefentry>
        <refentrytitle>drbd.conf</refentrytitle>

        <manvolnum>5</manvolnum>
      </citerefentry>, <citerefentry>
        <refentrytitle>drbd</refentrytitle>

        <manvolnum>8</manvolnum>
      </citerefentry>, <citerefentry>
        <refentrytitle>drbddisk</refentrytitle>

        <manvolnum>8</manvolnum>
      </citerefentry>, <citerefentry>
        <refentrytitle>drbdadm</refentrytitle>

        <manvolnum>8</manvolnum>
      </citerefentry>,
      <ulink url="http://www.drbd.org/users-guide/"><citetitle>DRBD User's Guide</citetitle></ulink>,
       <ulink url="http://www.drbd.org/"><citetitle>DRBD web site</citetitle></ulink></para>

  </refsect1>
</refentry>