<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE refentry PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd">
<refentry id="re-drbdconf">
  <refentryinfo>
    <date>6 May 2011</date>

    <productname>DRBD</productname>

    <productnumber>8.4.0</productnumber>
  </refentryinfo>

  <refmeta>
    <refentrytitle>drbd.conf</refentrytitle>

    <manvolnum>5</manvolnum> <refmiscinfo class="manual">設定ファイル</refmiscinfo> </refmeta> <refnamediv> <refname>drbd.conf</refname> <refpurpose>DRBD デバイスの設定ファイル<indexterm significance="normal">
        <primary>drbd.conf</primary>
      </indexterm></refpurpose> </refnamediv>

  <refsect1>
    <title>Introduction</title>

    <para><option>/etc/drbd.conf</option> ファイルは <option>drbdadm</option> コマンドから読み込まれる。</para>

    <para>本ファイルの形式は、クラスタを構成する 2 つのノード間でそのままコピーしても問題がないようにデザインされている。設定を管理しやすくするため、設定ファイルをそのままコピーすることを強く推奨する。<option>/etc/drbd.conf</option> ファイルは、クラスタを構成する 2 つのノードで同じ内容でなければならない。<option>/etc/drbd.conf</option> への変更はただちに反映されるものではない。 </para>

    <para>通例、メイン設定ファイルは 2 つのインクルードステートメントが含まれている。1 つ目は <option>/etc/drbd.d/global_common.conf</option> であり、2 つ目は <option>.res</option> の付いたすべてのファイルのサフィックスである。</para>

    <para>
        <title>簡単な .res ファイルの例</title>

        <programlisting format="linespecific">resource r0 {
	net {
		protocol C;
		cram-hmac-alg sha1;
		shared-secret &quot;FooFunFactory&quot;;
	}
	disk {
		resync-rate 10M;
	}
	on alice {
		volume 0 {
			device    minor 1;
			disk      /dev/sda7;
			meta-disk internal;
		}
		address   10.1.1.31:7789;
	}
	on bob {
		volume 0 {
			device    minor 1;
			disk      /dev/sda7;
			meta-disk internal;
		}
		address   10.1.1.32:7789;
	}
}</programlisting>上の例は r0 という名前の 1 つの DRBD リソースを設定している。ノード間の通信プロトコルは C である。ホスト alice では 1 つのボリューム含んでいて、 /dev/drbd1 をアプリケーション用のデバイスとして使用し、/dev/sda7 をデータ用の下位デバイスとしている。IP アドレスには 2 ノード間の通信に使うネットワークインタフェースの IP アドレスを指定している。DRBD の再同期には 10 メガバイト/秒の帯域幅を使うことができる。この resync-rate ステートメントはボリューム 0 に対して有効だが、追加のボリュームにも有効である。この例では各ボリュームに 10MB/秒フルで指定している。</para>

    <para>1つの drbd.conf に複数のリソースセクションを書くこともできる。他の設定例については <ulink url="http://www.drbd.org/users-guide/"><citetitle>DRBD User's Guide</citetitle></ulink> を参照</para>

  </refsect1>

  <refsect1>
    <title>File Format</title>

    <para>本ファイルはセクションとパラメータで構成される。各セクションはキーワードで始まり、任意指定のパラメータ、開き中括弧 (<quote>{</quote>) で始まる。閉じ中括弧 (<quote>}</quote>) でセクションが終わる。中括弧はパラメータを囲むのにも使われる。</para>

    <para>section [name] { parameter value; [...] }</para>

    <para>パラメータ名と値の間にホワイトスペースが必要である。パラメータ名の後ろの文字列はパラメータに対する値と見なされる。ブールパラメータは特別なパラメータで、パラメータ名だけで構成される。パラメータの最後にセミコロン (<quote>;</quote>) が必要である。</para>

    <para>いくつかのパラメータ値はデフォルトの単位を持つが、 K 、 M 、または G を明示的に指定することによって単位を変えられる。これらの単位はコンピュータでおなじみの方法で定義される (K=2^10=1024, M=1024K, G=1024M)。</para>

    <para>設定ファイルにコメントを記述できる。コメント行はハッシュ記号 (<quote>#</quote>) で始まらなければならない。この文字以降行末までの文字がコメントと見なされる。</para>

    <refsect2>
      <title>セクション</title>

      <variablelist>
        <varlistentry>
          <term xml:id="skip"><option>skip</option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>skip</secondary>
              </indexterm>このセクション内のテキストは、複数行にわたってすべてコメントと見なされる。キーワードの <option>skip</option> と開き中括弧 (<quote>{</quote>) の間にある文字は無視される。中括弧で囲まれたすべてが無視される。何か '<option>resource [name] {...}</option>' セクションをコメントアウトしたいとき、 '<option>skip</option>' を前に置けばよいので便利である。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="global"><option>global</option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>global</secondary>
              </indexterm>いくつかの全般的なパラメータを記述する。現在 <option>minor-count</option>, <option>dialog-refresh</option>, <option>disable-ip-verification</option>, <option>usage-count</option> が許可されている。global セクションは 1 回だけ記述でき、設定ファイルの先頭に書くのが望ましい。 </para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="common"><option>common</option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>common</secondary>
              </indexterm>common セクションに記述したパラメータはすべてのリソースに継承される。このセクションには、 <option>startup</option>, <option>options</option>, <option>handlers</option>, <option>net</option>, <option>disk</option> セクションがある。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="resource"><option>resource <replaceable>name</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>resource</secondary>
              </indexterm>DRBD リソースを定義するセクションである。各リソースセクションは、 2 つ (またはそれ以上) の <option>on <replaceable>host</replaceable></option> セクションを持つ必要があり、必要に応じて <option>startup</option>, <option>options</option>, <option>handlers</option>, <option>net</option>, <option>disk</option> セクションを書くことができる。<option>volume</option> セクションを含ませることもできる。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="on"><option>on <replaceable>host-name</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>on</secondary>
              </indexterm>このセクションを取り囲む resource セクションの中の DRBD デバイスの設定を指定する。<replaceable>host-name</replaceable> は必須で、各ノードの Linux ホスト名  (uname -n) でなければならない。複数のホスト名を指定しても構わないが、この場合、それぞれのホストは同じ設定でなければならない。ただし、 IP アドレスに関しては、複数のホストの間で変更する必要がある。もしくは、このセクションを複数指定できる。<programlisting format="linespecific">	resource r1 {
		protocol C;
		device minor 1;
		meta-disk internal;

		on alice bob {
			address 10.2.2.100:7801;
			disk /dev/mapper/some-san;
		}
		on charlie {
			address 10.2.2.101:7801;
			disk /dev/mapper/other-san;
		}
		on daisy {
			address 10.2.2.103:7801;
			disk /dev/mapper/other-san-as-seen-from-daisy;
		}
	}
	</programlisting><option>floating</option> セクションのキーワードも参照すること。このセクションでの必要なステートメントは <option>address</option> と <option>volume</option> である。上位互換性と利便性のため、直接ホストセクションに 1 つのボリュームのステートメントを埋め込むことが有効である。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="volume"><option>volume <replaceable>vnr</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>volume</secondary>
              </indexterm>コネクションの範囲でボリュームを定義する。別のホストでは、レプリケートされたボリュームのマイナー番号が異なっている事があるため、ボリューム番号 (<replaceable>vnr</replaceable>) はこれらを結びつける。このセクションで必要なパラメータは <option>device</option>, <option>disk</option>, <option>meta-disk</option> である。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="stacked-on-top-of"><option>stacked-on-top-of <replaceable>resource</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>stacked-on-top-of</secondary>
              </indexterm>3 または 4 ノード構成のときのスタックした DRBD リソース(上位リソース)を指定するときに、<option>on</option> セクションの代わりに <option>stacked-on-top-of</option> を指定する。このセクションには <option>device</option>, <option>address</option> が必須である。 </para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="floating"><option>floating <replaceable>AF addr:port</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>on</secondary>
              </indexterm>このセクションを取り囲む resource セクションの中の DRBD デバイスの設定を指定する。このセクションは、<option>on</option> セクションと非常に類似性がある。<option>on</option> セクションとの違いは、ホスト名ではなく IP アドレスでホスト名を照合することである。このセクションには、 <option>device</option>, <option>disk</option>, <option>meta-disk</option> が必要である。これらのパラメータを resource セクションに記述すると、このセクションはこれらの値を継承できる。すべてを継承する場合、 IP アドレスを指定するだけでよい。
<programlisting format="linespecific">	resource r2 {
		protocol C;
		device minor 2;
		disk      /dev/sda7;
		meta-disk internal;

		# short form, device, disk and meta-disk inherited
		floating 10.1.1.31:7802;

		# longer form, only device inherited
		floating 10.1.1.32:7802 {
			disk /dev/sdb;
			meta-disk /dev/sdc8;
		}
	}
	</programlisting></para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="s-disk"><option>disk</option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>disk</secondary>
              </indexterm>このセクションは、下位ストレージに対する DRBD の取り扱いをチューニングするパラメータで構成される。詳細は次のドキュメントを参照: <citerefentry>
                <refentrytitle>drbdsetup</refentrytitle>

                <manvolnum>8</manvolnum> </citerefentry> 。指定できるパラメータは次のとおり: <option>on-io-error</option>, <option>size</option>, <option>fencing</option>, <option>disk-barrier</option>, <option>disk-flushes</option>, <option>disk-drain</option>, <option>md-flushes</option>, <option>max-bio-bvecs</option>, <option>resync-rate</option>, <option>resync-after</option>, <option>al-extents</option>, <option>al-updates</option>, <option>c-plan-ahead</option>, <option>c-fill-target</option>, <option>c-delay-target</option>, <option>c-max-rate</option>, <option>c-min-rate</option>, <option>disk-timeout</option>, <option>discard-zeroes-if-aligned</option>, <option>rs-discard-granularity</option>, <option>read-balancing</option>.</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="net"><option>net</option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>net</secondary>
              </indexterm>このセクションは、DRBD のプロパティの取り扱いをチューニングするパラメータで構成される。詳細は次のドキュメントを参照: <citerefentry>
                <refentrytitle>drbdsetup</refentrytitle>

                <manvolnum>8</manvolnum> </citerefentry> 。指定できるパラメータは次のとおり: <option>protocol</option>, <option>sndbuf-size</option>, <option>rcvbuf-size</option>, <option>timeout</option>, <option>connect-int</option>, <option>ping-int</option>, <option>ping-timeout</option>, <option>max-buffers</option>, <option>max-epoch-size</option>, <option>ko-count</option>, <option>allow-two-primaries</option>, <option>cram-hmac-alg</option>, <option>shared-secret</option>, <option>after-sb-0pri</option>, <option>after-sb-1pri</option>, <option>after-sb-2pri</option>, <option>data-integrity-alg</option>, <option>no-tcp-cork</option>, <option>on-congestion</option>, <option>congestion-fill</option>, <option>congestion-extents</option>, <option>verify-alg</option>, <option>use-rle</option>, <option>csums-alg</option>, <option>socket-check-timeout</option>.</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="startup"><option>startup</option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>startup</secondary>
              </indexterm>このセクションは、DRBD のプロパティの取り扱いをチューニングするパラメータで構成される。詳細は次のドキュメントを参照: <citerefentry>
                <refentrytitle>drbdsetup</refentrytitle>

                <manvolnum>8</manvolnum> </citerefentry> 。指定できるパラメータは次のとおり: <option>wfc-timeout</option>, <option>degr-wfc-timeout</option>, <option>outdated-wfc-timeout</option>, <option>wait-after-sb</option>, <option>stacked-timeouts</option> and <option>become-primary-on</option>.</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="options"><option>options</option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>options</secondary>
              </indexterm>このセクションはリソースオブジェクトの取り扱いをチューニングするパラメータで構成される。詳細は次のドキュメントを参照: <citerefentry>
                <refentrytitle>drbdsetup</refentrytitle>

                <manvolnum>8</manvolnum> </citerefentry> 。指定できるパラメータは次のとおり: <option>cpu-mask</option>, and <option>on-no-data-accessible</option>.</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="handlers"><option>handlers</option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>handlers</secondary>
              </indexterm>このセクションでは、特定のイベントへに応答して DRBD によって起動される（実行可能な）ハンドラを指定できる。指定できるパラメータは次のとおり: <option>pri-on-incon-degr</option>, <option>pri-lost-after-sb</option>, <option>pri-lost</option>, <option>fence-peer</option> (formerly oudate-peer), <option>local-io-error</option>, <option>initial-split-brain</option>, <option>split-brain</option>, <option>before-resync-target</option>, <option>after-resync-target</option>.</para>

            <para>環境変数がインタフェースとなる:<itemizedlist>
                <listitem>
                  <para><option>DRBD_RESOURCE</option> はリソース名である。</para>
                </listitem>

                <listitem>
                  <para><option>DRBD_MINOR</option> は DRBD デバイスの 10 進法のマイナー番号である。</para>
                </listitem>

                <listitem>
                  <para><option>DRBD_CONF</option> はプライマリ設定ファイルへの入り口である。設定を複数ファイルに分割した場合 (例えば <option>/etc/drbd.conf.d/</option> など) には、あまり役にたたない。</para>
                </listitem>

                <listitem>
                  <para><option>DRBD_PEER_AF</option> , <option>DRBD_PEER_ADDRESS</option> , <option>DRBD_PEERS</option> はそれぞれ、アドレスファミリ (ipv6 など)、対向ホストのアドレス、対向ホストのホスト名となる。 </para>
                </listitem>
              </itemizedlist> <option>DRBD_PEER</option> は推奨しない。</para>

            <para>これらの設定がすべてのハンドラに設定されるわけではない。いくつかの値は <option>floating</option> 設定では無効になる。 
</para>
          </listitem>
        </varlistentry>
      </variablelist>
    </refsect2>

    <refsect2>
      <title>パラメータ</title>

      <variablelist>
        <varlistentry>
          <term xml:id="minor-count"><option>minor-count <replaceable>count</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>minor-count</secondary>
              </indexterm><replaceable>count</replaceable> には 1 から 1048575 の値を指定できる。</para>

            <para><replaceable>Minor-count</replaceable> は DRBD のサイジングための手がかりである。これは様々なメモリプールを適正サイズにするのに役立つ。実際に使っているマイナー番号よりも大きな数を順に指定しなければいけない。デフォルトでは、現在定義されているリソースの数に加えてさらに 11 個のリソースを定義できる。ただし最低でも 32 個は定義できる。 </para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="dialog-refresh"><option>dialog-refresh <replaceable>time</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>dialog-refresh</secondary>
              </indexterm><replaceable>time</replaceable> は 0 または正の数を指定する。</para>

            <para>ユーザダイアログは <replaceable>time</replaceable> 秒ごとに秒を作画する (<replaceable>time</replaceable> が 0 だと再作画しない)。デフォルト値は 1 である。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="disable-ip-verification"><option>disable-ip-verification</option></term>

          <listitem>
            <indexterm significance="normal">
              <primary>drbd.conf</primary>

              <secondary>disable-ip-verification</secondary>
            </indexterm>

            <para>何らかの理由で drbdadm が <option>ip</option> または <option>ifconfig</option> コマンドで正常さを確認できない場合 <replaceable>disable-ip-verification</replaceable> を指定する。このオプションを指定することによって、 IP アドレスを検証しないようにできる。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="udev-always-use-vnr"><option>udev-always-use-vnr</option></term>

          <listitem>
            <indexterm significance="normal">
              <primary>drbd.conf</primary>

              <secondary>udev-always-use-vnr</secondary>
            </indexterm>

      <para>udev が drbdadm にデバイス関連のシンボリックリンクのリストを要求すると、drbdadm は、リソースに明示的な <literal>volume VNR { }</literal> 定義があるか、暗黙的なボリューム番号 0 を持つ単一のボリュームしかないかによって、異なる命名規則でシンボリックリンクを提示する：<programlisting><![CDATA[
# implicit single volume without "volume 0 {}" block
DEVICE=drbd<minor>
SYMLINK_BY_RES=drbd/by-res/<resource-name>

# explicit volume definition: volume VNR { }
DEVICE=drbd<minor>
SYMLINK_BY_RES=drbd/by-res/<resource-name>/VNR
]]></programlisting>
	</para>

	<para>global セクションでこのパラメータを定義すると、drbdadm は常に <literal>.../VNR</literal> の部分を追加し、ボリューム定義が暗黙的であるか明示的であるかを気にしない。</para>

	<para>過去との互換性のために、これはデフォルトでは無効になっているが、有効にすることを推奨する。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="usage-count"><option>usage-count <replaceable>val</replaceable></option></term>

          <listitem>
            <indexterm significance="normal">
              <primary>drbd.conf</primary>

              <secondary>usage-count</secondary>
            </indexterm>

            <para><ulink url="http://usage.drbd.org"><citetitle>DRBD の利用者統計</citetitle></ulink> に参加する。最も簡単な方法は、このオプションを <option>yes</option> に指定する。指定できる値は <option>yes</option>, <option>no</option>, <option>ask</option> である。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="protocol"><option>protocol <replaceable>prot-id</replaceable></option></term>

          <listitem>
            <indexterm significance="normal">
              <primary>drbd.conf</primary>

              <secondary>protocol</secondary>
            </indexterm>

            <para>DRBD の TCP/IP の通信の際に使われる <replaceable>プロトコル</replaceable> を指定する。指定できるプロトコルは A, B, C である。</para>

            <para>プロトコル A: ローカルディスクとローカル TCP 送信バッファにデータを書き込んだらディスクへの書き込みが完了したと判断する。</para>

            <para>プロトコル B: ローカルディスクとリモートバッファキャッシュにデータを書き込んだらディスクへの書き込みが完了したと判断する。</para>

            <para>プロトコル C: ローカルディスクとリモートディスクの両方にデータを書き込んだらディスクへの書き込みが完了したと判断する。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="device"><option>device <replaceable>name</replaceable> minor
          <replaceable>nr</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>device</secondary>
              </indexterm>定義している DRBD リソースに対応するブロックデバイス名を指定する。アプリケーションでは、ここで指定する名前をデバイス名として指定する（ファイルシステムとして）。逆に、<option>disk</option> に指定したデバイス名を決して指定してはならない。</para>

            <para><replaceable>name</replaceable>, <option>minor</option>, <replaceable>minor number</replaceable> は省略する事ができる。<replaceable>name</replaceable> を省略すると、デフォルトの /dev/drbd<replaceable>minor</replaceable> が使われる。</para>

            <para>udev は自動的に /dev/drbd/by-res と /dev/drbd/by-disk の中のシンボリックリンクを作成する。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="disk"><option>disk <replaceable>name</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>disk</secondary>
              </indexterm>DRBD はこのブロックデバイスに実際にデータの読み書きを行う。DRBD 動作中は、このデバイスに絶対に別の方法でアクセスしてはならない。この禁止事項には、次のコマンドやその他の類似コマンドも含まれる: <citerefentry>
                <refentrytitle>dumpe2fs</refentrytitle>

                <manvolnum>8</manvolnum> </citerefentry>。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="address"><option>address <replaceable>AF addr:port</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>address</secondary>
              </indexterm>それぞれが各対向デバイスからの接続を待ち受けるために、 デバイスごとに全てのリソースに <replaceable>IP</replaceable> アドレス が必要である。<replaceable>AF</replaceable> は <option>ipv4</option>, <option>ipv6</option>, <option>ssocks</option> or <option>sdp</option> (互換性のため <option>sci</option> は<option>ssocks</option> へのエイリアスになっている) のなかから選択される。IPv4 の場合、これを省略できる。IPv6 を使う場合は、 <option>ipv6</option> のキーワードに続けて角括弧の中に記載する。例: <literal moreinfo="none">ipv6 [fd01:2345:6789:abcd::1]:7800</literal>。</para>

            <para>各 DRBD リソースは、ノードのパートナーデバイスと接続するために TCP <replaceable>port</replaceable> が必要である。2 つの別の DRBD リソースが同じ <replaceable>addr:port</replaceable> の組み合わせを同一ノードで使用することはできない</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="meta-disk"><option>meta-disk internal</option></term>

          <term><option>meta-disk <replaceable>device</replaceable></option></term>

	  <term><option>meta-disk <replaceable>device</replaceable> [<replaceable>index</replaceable>]</option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>meta-disk</secondary>
              </indexterm>内部 (internal) を指定すると、下位デバイスの最後の部分にメタデータが作られる。メタデータのサイズはデバイスのサイズから計算される。</para>

            <para><replaceable>device</replaceable> が指定されると、 <replaceable>index</replaceable> がある場合でもない場合でも、 DRBD はそのデバイスにメタデータを保存する。<replaceable>index</replaceable> がない場合、メタデータのサイズはデータデバイスのサイズから決定される。このキーワードは通常 LVM と組み合わせて使用され、様々なサイズのブロックデバイスを扱うときに使用する。メタデータのサイズは 36KB + 下位デバイスのサイズ / 32K であり、4KB 単位で切り上げる。(おおまかな目安として、ストレージサイズ 1GB あたり 32KB のメタデータ領域が必要で、これを MB 単位に切り上げる)。</para>

	    <para> <replaceable>index</replaceable> が指定されると、各インデックス数は 128MB のメタデータスロットを参照する。これは最大 4TiB まで指定することができる。この方法により複数の DBRD デバイスが同一のメタデータデバイスを共有できるようになる。例えば /dev/sde6[0] と /dev/sde6[1] を使用するとき、 /dev/sde6 は最低でも 256MB 以上でなければいけない。ハードサイズの制限のため、メタディスクのインデックスは推奨されない。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="on-io-error"><option>on-io-error <replaceable>handler</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>on-io-error</secondary>
              </indexterm>下位デバイスが I/O エラーを上位に伝えたときに実行する <replaceable>handler</replaceable> を指定する。</para>

            <para><replaceable>handler</replaceable> には <option>pass_on</option>, <option>call-local-io-error</option>, <option>detach</option> のいずれかを指定できる。</para>

            <para><option>pass_on</option>: ノードはディスクのステータスを inconsistent(不整合) にし、 I/O エラーを起こしたブロックに対応するビットマップにマークをつける。そして、リモートのノード上で入出力を再度行う。</para>

            <para><option>call-local-io-error</option>: ハンドラスクリプト <option>local-io-error</option> を呼び出して実行する。</para>

            <para><option>detach</option>: 低レベルデバイスを切り離して、ディスクレスモードで処理を続行する。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="fencing"><option>fencing <replaceable>fencing_policy</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>fencing</secondary>
              </indexterm><option>fencing</option> は、2 つのノードがともにプライマリで切り離された状態 (スプリットブレイン) になる事を防ぐ手段である。</para>

            <para>次のフェンシングポリシーを指定できる: </para>

            <variablelist>
              <varlistentry>
                <term xml:id="dont-care"><option>dont-care</option></term>

                <listitem>
                  <para>デフォルトの設定値で、フェンシングのためのアクションを実行しない。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term xml:id="resource-only"><option>resource-only</option></term>

                <listitem>
                  <para>ノードが切り離されたプライマリ状態になると、他ノードを無効状態に変えようとする。この動作は <option>fence-peer</option> ハンドラによって行われる。このハンドラは他ノードにレプリケーション用とは別のネットワーク経由でアクセスし、'<option>drbdadm outdate res</option>' を実行することを想定している。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term xml:id="resource-and-stonith"><option>resource-and-stonith</option></term>

                <listitem>
                  <para>ノードが切り離されたプライマリ状態になると、 DRBD はすべてのディスク I/O を停止して fence-peer ハンドラを呼び出す。このハンドラには、レプリケーション用とは別のネットワーク経由で他ノードにアクセスし、 'drbdadm outdate res' を実行するという機能を想定している。ハンドラが他ノードに到達できない場合、 DRBD は STONITH 機能を使って他ノードを強制排除する。これらが完了したら、ディスク I/O を再開する。ハンドラが失敗した場合には、 <option>resume-io</option> コマンドでディスク I/O を再開できる。</para>
                </listitem>
              </varlistentry>
            </variablelist>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="disk-barrier"><option>disk-barrier</option></term>

          <term xml:id="disk-flushes"><option>disk-flushes</option></term>

          <term xml:id="disk-drain"><option>disk-drain</option></term>

          <listitem>
            <indexterm significance="normal">
              <primary>drbd.conf</primary>
              <secondary>disk-barrier</secondary>
            </indexterm>

            <indexterm significance="normal">
              <primary>drbd.conf</primary>

              <secondary>disk-flushes</secondary>
            </indexterm>

            <indexterm significance="normal">
              <primary>drbd.conf</primary>
              <secondary>disk-drain</secondary>
            </indexterm>

            <para>DRBD は下位デバイスに対する複数のディスク書き込みの間の依存関係を指定するための 4 種類のオプションを用意している。そのうち、下位デバイスがサポートしていてユーザが無効に設定していない最初のオプションが使用される。デフォルトでは <emphasis>flush</emphasis> メソッドが使われる。</para>

	    <para>drbd-8.4.2 から <option>disk-barrier</option> は、linux-2.6.36 (及び RHEL6 の 2.6.32) 以降での正常動作が確認できないためデフォルトで無効になった。注意: 有識者による指示のもとでのみ使用すること。</para>

            <para>手法を選択するにあたっては、測定可能なパフォーマンスデータのみに頼るべきではない。下位デバイスが揮発性の書き込みキャッシュしか持たない場合 (通常のハードディスクや通常のハードディスクだけで構成される RAID など)、最初の 2 つのオプションのどちらかを使うべきである。下位デバイスにバッテリバックアップ機能付きの書き込みキャッシュがある場合には、3 番目のオプションが利用できる。4番目のオプション (すべてを無効にする場合は &quot;none&quot;) は、ほとんどの I/O スタックにおいて危険で、ディスクへの書き込み順序が入れ替わってしまう可能性がある。<option>no-disk-drain</option> を使っては <emphasis>ならない </emphasis>。</para>

            <para>残念なことに、デバイスマッパ(LVM) はバリアをサポートしていない。</para>

            <para>/proc/drbd の &quot;wo:&quot; の文字の後ろに、下位デバイスに対する現在の設定が <option>b</option>, <option>f</option>, <option>d</option>, <option>n</option> の文字で表示される。オプションは次のとおり: </para>

            <variablelist>
              <varlistentry>
                <term>barrier</term>

                <listitem>
                  <para>下位デバイスがバリア (SCSI では &quot;tagged command queuing&quot;、SATA では &quot;native com-mand queuing&quot; と呼ばれる) をサポートしている場合、このオプションを選択できる。このオプションを有効にするには <option>disk-barrier</option> オプションを <option>yes</option> にする。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term>flush</term>

                <listitem>
                  <para>下位デバイスがディスクフラッシュ (ベンダーは &quot;force unit access&quot; と呼んでいる) をサポートしている場合、このオプションを選択できる。このオプションを無効にするには <option>disk-flushes</option> を <option>no</option> に設定する。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term>drain</term>

                <listitem>
                  <para>３番目の方法は、単純に、最初の書き込みは次の書き込みリクエストを処理する前に吐き出す、方法である。8.0.9 まではこれが唯一のオプションであった。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term>none</term>

                <listitem>
                  <para>４番目の方法は、<option>no-disk-drain</option> を指定し、下位デバイスへの書き込みの依存関係を一切指示しない方法である。これはほとんどの I/O スタックにおいて <emphasis>危険</emphasis> であり、ディスクへの書き込み順序が入れ替わってしまう可能性がある。そうなると、理論的にはデータ破損の原因、または DRBD プロトコルを乱し、再接続と切断を繰り返すような状態に陥る可能性がある。<option>no-disk-drain</option> を <emphasis>使ってはならない</emphasis>。</para>
                </listitem>
              </varlistentry>
            </variablelist>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="md-flushes"><option>md-flushes</option></term>

          <listitem>
            <indexterm significance="normal">
              <primary>drbd.conf</primary>

              <secondary>md-flushes</secondary>
            </indexterm>

            <para>メタデータデバイスへのアクセスにあたってバリアやフラッシュを使用しない。 <option>disk-flushes</option> についての説明を参照のこと。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="max-bio-bvecs"><option>max-bio-bvecs</option></term>

          <listitem>
            <indexterm significance="normal">
              <primary>drbd.conf</primary> <secondary>max-bio-bvecs</secondary>。
            </indexterm>

            <para>ある特別な環境において、デバイスマッパースタックは、複数の bvec を持つ制約に違反する BIO を merge_bvec（） 関数の４番目の引数に設定し DRBD に渡す。 例えば、物理ディスク → DRBD → LVM → Xen → 誤ったパーティション(63) → DomU FS の場合である。Dom0 のカーネルログには、 &quot;bio would need to, but cannot, be split:&quot; と記録される。</para>

            <para>最も良い回避方法は、VM の内部にパーティションを適切に配置する (例えば、セクタ 1024 から開始する) ことである。これは、ストレージ領域を 480 KiB を消費する。残念ながら、ほとんどの Linux パーティションツールは、奇数 (63) でパーティションを開始する。そのため、ほとんどのディストリビューションは、仮想 Linux マシンにインストールを行うと、誤ったパーティションで終了してしまう。第 2 の回避方法は、 BIO あたりの最大 DRBD bvecs (= <option>max-bio-bvecs</option>) を 1 にすることである。しかし、パフォーマンスは低下する。</para>

            <para><option>max-bio-bvecs</option> のデフォルト値は 0 で、これはユーザに制限が無いことを意味する。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term>
            <option>disk-timeout</option>
          </term>
          <listitem>
            <indexterm significance="normal">
              <primary>drbd.conf</primary>
              <secondary>disk-timeout</secondary>
            </indexterm>
      <para>DRBD デバイスがデータを格納する下位レベルデバイスが、指定した <option>disk-timeout</option> 以内で I/O リクエストを完了しない場合、DRBD はこれを障害とみなす。下位デバイスは切り離され、デバイスのディスク状態はディスクレス状態になる。DRBD が 1 台以上の対向ノードに接続したとき、失敗したリクエストはそのうち 1 台に伝えられる。</para>

      <para>このオプションは <emphasis>カーネルパニックを引き起こす可能性があり、注意が必要である</emphasis>。</para>

      <para>リクエストの「中断」あるいはディスクの強制切り離しは、完全に下位デバイスをブロックまたはハンギングして、リクエストをまったく処理せずエラーも処理しなくなる。この状況ではハードリセットとフェイルオーバ以外になす術がない。</para>

      <para>「中断」すると、基本的にローカルエラーの完了を装い、すみやかにサービスの移行を行うことで安全な切り替えを行う。それでもなお、影響を受けるノードは &quot;すぐ&quot; に再起動される必要はある。</para>
      <para>リクエストを完了することで、上位レイヤーに関連するデータページを再利用させることができる。</para>

      <para>後にローカルの下位デバイスが「復帰」すると、ディスクから元のリクエストページへの DMA のデータは、うまくいくと未使用のページへランダムなデータを送るが、多くの場合その間に関係のないデータに変形してしまい、様々なダメージの原因になる。</para>

      <para>つまり遅延した正常な完了は、特に読み込みリクエストの場合 panic() の原因になる。遅延した「エラー」完了は、その都度に通知は行うが、問題ないと考えてよい。</para>
      <para><option>disk-timeout</option> のデフォルト値は 0 であり、無限のタイムアウトを意味する。タイムアウトは 0.1 秒単位で指定する。このオプションは DRBD 8.3.12. から利用できる。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="discard-zeroes-if-aligned"><option>discard-zeroes-if-aligned <group choice="req" rep="norepeat">
	     <arg choice="plain" rep="norepeat">yes</arg>
	     <arg choice="plain" rep="norepeat">no</arg>
	  </group></option></term>
          <listitem>
	    <para>
            <indexterm significance="normal">
              <primary>drbd.conf</primary>
              <secondary>discard-zeroes-if-aligned</secondary>
            </indexterm>Linux のブロックデバイスで discard/trim/unmap のサポートにはいくつかの側面がある。discard が一般的にサポートされていても、暗黙に失敗したり、discard 要求を部分的に無視したりすることがある。デバイスは、また、マップされていないブロックからの読み込みが、定義済みのデータ（通常はゼロ）、未定義のデータ（おそらく古いデータか、ゴミ）のどちらを返すか通知する。</para><para>異なるノードで DRBD が discard 特性が異なるデバイスによって構成されている場合、discard はデータの不一致（古いデータまたはゴミが 1 つのバックエンドに残り、別のバックエンドではゼロが残る）の原因となる。オンライン照合は、数多くの偽の差異を報告する可能性がある。たぶんほとんどのユースケース （ファイルシステム上の fstrim） では無害であるが、DRBD はそれを持つことはできません。</para><para>安全に動作させるには、ローカルのバックエンド（プライマリ上）が &quot;discard_zeroes_data=true&quot; をサポートしていない場合、 discard のサポートを無効にする必要がある。受信側（セカンダリ）がマップされていなかった領域を割り当て、 &quot;discard_zeroes_data = true&quot; をサポートしていない場合、受信側で discard を明示的にゼロに変換する必要がある。</para><para>discard をサポートしているのに、discard_zeroes_data = false をアナウンスするデバイス（特に LVM/DM シンプロビジョニング）がある。DM-thin の場合、チャンクサイズに合わせた discard はマップされず、マッピングされていないセクタからの読み込みはゼロを返す。ただし、discard 要求のアライメントされていない部分ヘッドまたはテール領域は暗黙に無視される。</para><para>整列したフル・チャンクの discard をパスし、これらの整列していない部分領域を明示的にゼロ・アウトするヘルパーを追加すると、そのようなデバイスでは discard_zeroes_data = true を効果的に達成する。</para><para><option> discard-zeroes-if-aligned </option> を <option>yes</option> に設定すると、 discard_zeroes_data = false を通知するバックエンドであっても DRBD は discard を使用し、 discard_zeroes_data = true を通知する。</para><para><option> discard-zeroes-if-aligned </option> を <option>no</option> に設定すると、それぞれのバックエンドが discard_zeroes_data = false をアナウンスする場合、DRBD は常に受信側でゼロアウトにフォールバックし、プライマリ側では discard に関して通知しない。</para><para>私たちは、 discard_zeroes_data 設定を完全に無視していました。確立し、期待された動作を壊さず、シンプロビジョニング LV の fstrim がスペースを解放する代わりにスペースを使い果たさないためのデフォルト値は <option>yes</option> である。</para><para>このオプションは 8.4.7 から有効である。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term>
            <option>read-balancing <replaceable>method</replaceable></option>
          </term>
          <listitem>
            <indexterm significance="normal">
              <primary>drbd.conf</primary>
              <secondary>read-balancing</secondary>
            </indexterm>
	    <para>読み込みリクエストの負荷分散で使用できる <replaceable>methods</replaceable> は <option>prefer-local</option>, <option>prefer-remote</option>, <option>round-robin</option>, <option>least-pending</option>, <option>when-congested-remote</option>, <option>32K-striping</option>, <option>64K-striping</option>, <option>128K-striping</option>, <option>256K-striping</option>, <option>512K-striping</option>, <option>1M-striping</option> である。</para>
	      <para>デフォルトは <option>prefer-local</option> である。このオプションは 8.4.1 から有効である。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="rs-discard-granularity">
            <option>rs-discard-granularity <replaceable>byte</replaceable></option>
          </term>
          <listitem>
            <indexterm significance="normal">
              <primary>drbd.conf</primary>
              <secondary>rs-discard-granularity</secondary>
            </indexterm>
	    <para><option> rs-discard-granularity </option> がゼロ以外の正の値に設定されている場合、DRBD はこのサイズで再同期操作を要求する。そのようなブロックが同期ソースノード上にゼロバイトしか含まない場合、同期ターゲットノードは、その領域に対して discard/trim/unmap コマンドを発行する。</para>
	      <para>この値は、下位ブロックデバイスの discard 粒度によって制約される。<option> rs-discard-granularity </option>が下位ブロックデバイスの discard 粒度の乗数でない場合、DRBD はそれを切り上げる。この機能は、下位ブロックデバイスが discard コマンドの後に、ゼロを読み戻す場合にのみアクティブになる。</para>
	      <para>デフォルト値は 0 である。このオプションは 8.4.7 から有効である。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="sndbuf-size"><option>sndbuf-size <replaceable>size</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>sndbuf-size</secondary>
              </indexterm><replaceable>size</replaceable> は TCP ソケットのセンドバッファである。デフォルト値は 0 で自動調整される。これより小さい値も大きい値も指定できる。遅延が大きいネットワークに対してプロトコル A を指定する場合に大きな値を指定すると、書き込みスループットを向上できる。32K より小さい値は現実的ではない。8.0.13 から 8.2.7 までは、手動で 0 を設定しないと自動調整にならない。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="rcvbuf-size"><option>rcvbuf-size <replaceable>size</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>rcvbuf-size</secondary>
              </indexterm><replaceable>size</replaceable> は TCP ソケットの受信バッファサイズである。デフォルト値は 0 で自動調整される。これより小さい値も大きい値も指定できる。通常は、このオプションはデフォルト値のまま運用する。<replaceable>size</replaceable> に 0 指定すると、カーネルが自動的に設定したバッファを使う。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="timeout"><option>timeout <replaceable>time</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>timeout</secondary>
              </indexterm>対向ノードからの応答パケットが 1/10 の time 倍の時間以内に返ってこない場合、対向ノードが死んだと判断して TCP/IP コネクションを切断する。この値は <replaceable>connect-int</replaceable> および <replaceable>ping-int</replaceable> よりも小さくなければならない。デフォルト値は 60 で、これは 6 秒に相当する。すなわちこのパラメータの単位は 0.1 秒である。 </para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="connect-int"><option>connect-int <replaceable>time</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>connect-int</secondary>
              </indexterm>対向ノードにただちに接続できない場合、DRBD は接続を繰り返し試行する。このパラメータは試行間隔を指定する。デフォルト値は 10 で、このパラメータの単位は秒である。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="ping-int"><option>ping-int <replaceable>time</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>ping-int</secondary>
              </indexterm>DRBD デバイス間の TCP/IP 接続があり、対向ノードから <replaceable>time</replaceable> 秒の間に何も通信が行われなかった場合、 DRBD は死活確認のためキープアライブパケットを生成する。デフォルト値は 10 で、このパラメータの単位は秒である。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="ping-timeout"><option>ping-timeout <replaceable>time</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>ping-timeout</secondary>
              </indexterm>このパラメータで指定した時間内にキープアライブパケットに応答しなければならない。応答パケットが返ってこない場合、その対向ノードは死んだと判断される。デフォルト値は 500ms で、100ms 単位で指定する。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="max-buffers"><option>max-buffers <replaceable>number</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>max-buffers</secondary>
              </indexterm>再同期、オンライン照合を行う際に、受信側で DRBD マイナーデバイスあたりに使用するメモリを制限する。単位は PAGE_SIZE で、ほとんどのシステムで 4KiB である。設定できる最小値は 32 (=128 KiB) でハードコードされている。これらバッファはディスクからの読み書きの際にデータブロックを保持するために使用される。輻輳時のデッドロックを回避するために、この設定はハード制限というよりは閾値として使用される。最大バッファページが使用されると、プールからのそれ以上の割り当てが制限される。受信側の I/O バックエンドに余裕がない場合には、 max-buffers を増やすとよい。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="ko-count"><option>ko-count <replaceable>number</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>ko-count</secondary>
              </indexterm>セカンダリノードが 1 回の書き込みリクエストに <replaceable>count</replaceable> 回以上失敗した場合、そのセカンダリノードはクラスタから排除される。（つまり、プライマリノードが接続をいったん終了し、再接続する）。この機能を無効にするには、明示的に 0 に設定する必要がある。デフォルトはバージョン間で変更されている。 8.4 は 7 がデフォルト値である。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="max-epoch-size"><option>max-epoch-size <replaceable>number</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>max-epoch-size</secondary>
              </indexterm>書き込みバリア間に処理するデータブロックの最大数を指定する。10 未満の値を指定するとパフォーマンスが低下することがある。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="allow-two-primaries"><option>allow-two-primaries</option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>allow-two-primaries</secondary>
              </indexterm>このオプションを指定すると、両ノードにプライマリを割り当てられる。このオプションは分散共有ファイルシステムを使うときのみ指定する。現在 DRBD がサポートするファイルシステムは OCFS2 と GFS である。これら以外のファイルシステムを使うときにこのオプションを指定すると、データの破損とノードのダウンを引き起こす。 </para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="unplug-watermark"><option>unplug-watermark <replaceable>number</replaceable></option></term>

          <listitem>
            <indexterm significance="normal">
              <primary>drbd.conf</primary>

              <secondary>unplug-watermark</secondary>
            </indexterm>
            <para>この設定は、最近の明示的にスタックプラギングを使用するカーネルには効果がない (Linux kernel 2.6.39 には移植されている)。</para>
            <para>スタンバイ(セカンダリ) ノードで書き込まれていない書き込みリクエスト数が unplug-watermark を上回ると、下位デバイスに対して書き込みリクエストを送る。ストレージによっては小さい値でも良好な結果が得られるが、多くのデバイスでは max-buffers と同じ値を指定するときに最良の結果が得られる。デフォルト値は 128 で、指定できる最小値は 16、最大値は 131072 である。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="cram-hmac-alg"><option>cram-hmac-alg</option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>cram-hmac-alg</secondary>
              </indexterm>対向ノードの認証を行いたい場合、 +HMAC アルゴリズムを指定する。対向ノードの認証は行うべきである。チャレンジ-レスポンス方式で対向ノードを認証するのに、 HMA CFC アルゴリズムが使われる。<option>/proc/crypto</option> に記録されている任意のダイジェストアルゴリズムを指定できる。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="shared-secret"><option>shared-secret</option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>shared-secret</secondary>
              </indexterm>対向ノードの認証には共有秘密鍵が使用され、64文字までで指定する。<option>cram-hmac-alg</option> を指定しないと対向ノードの認証は行われない。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="after-sb-0pri"><option>after-sb-0pri </option> <replaceable>policy</replaceable></term>

          <listitem>
            <indexterm significance="normal">
              <primary>drbd.conf</primary>

              <secondary>after-sb-0pri</secondary>
            </indexterm>

            <para>指定できるポリシーは以下の通り:</para>

            <variablelist>
              <varlistentry>
                <term xml:id="disconnect"><option>disconnect</option></term>

                <listitem>
                  <para>自動再同期を行わず接続を切断する。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term xml:id="discard-younger-primary"><option>discard-younger-primary</option></term>

                <listitem>
                  <para>スプリットブレイン発生前にプライマリであったノードからの再同期を自動的に実行する。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term xml:id="discard-older-primary"><option>discard-older-primary</option></term>

                <listitem>
                  <para>スプリットブレイン発生時にプライマリになったノードからの再同期を自動的に実行する。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term xml:id="discard-zero-changes"><option>discard-zero-changes</option></term>

                <listitem>
                  <para>プリットブレイン発生後どちらか一方のノードに書き込みがまったく行われなかったことが明白な場合、書き込みが行われたノードから行われなかったノードに対する再同期が実行される。どちらも書き込まれなかった場合は、 DRBD はランダムな判断によって 0 ブロックの再同期を実行する。両ノードに書き込みが行われた場合、このポリシーはノードの接続を切断する。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term xml:id="discard-least-changes"><option>discard-least-changes</option></term>

                <listitem>
                  <para>スプリットブレイン発生後、より多くのブロックを書き込んだノードから他方に対する再同期を実行する。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term xml:id="discard-node-NODENAME"><option>discard-node-NODENAME</option></term>

                <listitem>
                  <para>指定した名前のノードに対する再同期を実行する。</para>
                </listitem>
              </varlistentry>
            </variablelist>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="after-sb-1pri"><option>after-sb-1pri </option> <replaceable>policy</replaceable></term>

          <listitem>
            <indexterm significance="normal">
              <primary>drbd.conf</primary>

              <secondary>after-sb-1pri</secondary>
            </indexterm>

            <para>指定できるポリシーは以下の通り:</para>

            <variablelist>
              <varlistentry>
                <term xml:id="sb1-disconnect"><option>disconnect</option></term>

                <listitem>
                  <para>自動再同期を行わず接続を切断する。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term xml:id="consensus"><option>consensus</option></term>

                <listitem>
                  <para><option>after-sb-0pri</option> アルゴリズムの結果が現在のセカンダリノードのデータを壊すことになる場合、セカンダリノードのデータを捨てる。そうではない場合は接続を切断する。そうではない場合は接続を切断する。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term xml:id="sb1-violently-as0p"><option>violently-as0p</option></term>

                <listitem>
                  <para>プライマリのデータに大きな変更がある場合でも、常に <option>after-sb-0pri</option> アルゴリズムの判断を採用する。このポリシーは <option>allow-two-primaries</option> オプションを指定したうえで 1 ノードファイルシステム (OCF2 や GFS ではない) を使用し、かつ十分に理解している場合のみ有用である。プライマリノードでファイルシステムをマウントしている場合、<emphasis>このポリシーは危険であり、マシンを破壊する可能性がある</emphasis>。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term xml:id="discard-secondary"><option>discard-secondary</option></term>

                <listitem>
                  <para>セカンダリ側のデータを捨てる。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term xml:id="sb1-call-pri-lost-after-sb"><option>call-pri-lost-after-sb</option></term>

                <listitem>
                  <para><option>after-sb-0pri</option> アルゴリズムの判断を常に採用する。セカンダリ側のデータが正しいと判断された場合には、現在のプライマリ側で &quot;pri-lost-after-sb&quot; ハンドラが呼び出される。</para>
                </listitem>
              </varlistentry>
            </variablelist>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="after-sb-2pri"><option>after-sb-2pri </option> <replaceable>policy</replaceable></term>

          <listitem>
            <indexterm significance="normal">
              <primary>drbd.conf</primary>

              <secondary>after-sb-2pri</secondary>
            </indexterm>

            <para>指定できるポリシーは以下の通り:</para>

            <variablelist>
              <varlistentry>
                <term xml:id="sb2-disconnect"><option>disconnect</option></term>

                <listitem>
                  <para>自動再同期を行わず接続を切断する。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term xml:id="sb2-violently-as0p"><option>violently-as0p</option></term>

                <listitem>
                  <para>プライマリのデータに大きな変更がある場合でも、常に <option>after-sb-0pri</option> アルゴリズムの判断を採用する。このポリシーは <option>allow-two-primaries</option> オプションを指定したうえで 1 ノードファイルシステム (OCF2 や GFS ではない) を使用し、かつ十分に理解している場合のみ有用である。プライマリノードでファイルシステムをマウントしている場合、<emphasis>このポリシーは危険であり、マシンを破壊する可能性がある</emphasis>。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term xml:id="sb2-call-pri-lost-after-sb"><option>call-pri-lost-after-sb</option></term>

                <listitem>
                  <para>どちらか一方のマシンで &quot;pri-lost-after-sb&quot; ハンドラを呼び出す。このプログラムには、マシンをリブートしてそのマシンをセカンダリにするような機能が要求される。</para>
                </listitem>
              </varlistentry>
            </variablelist>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="always-asbp"><option>always-asbp</option></term>

          <listitem>
            <para>通常、3 番目のノードが存在しないことが現在の UUID 値から明らかな場合のみ、スプリットブレイン発生後の修復ポリシーだけが適用される。</para>

            <para>このオプションを指定すると、両ノードのデータに関連性があるとして、スプリットブレイン発生後のポリシーが適用される。UUID の分析により 3 番目のノードの存在が疑われる場合には、フル同期が行われることがある。(または、なんらかの別の原因によって間違った UUID セットで判断してしまった場合)</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="rr-conflict"><option>rr-conflict </option> <replaceable>policy</replaceable></term>

          <listitem>
            <indexterm significance="normal">
              <primary>drbd.conf</primary>

              <secondary>rr-conflict</secondary>
            </indexterm>

            <para>このオプションは、再同期決定の結果がクラスタ内の現在のロール割り当てと互換性がない場合を解決するのに役立つ。</para>

            <variablelist>
              <varlistentry>
                <term xml:id="rr-disconnect"><option>disconnect</option></term>

                <listitem>
                  <para>自動再同期を行わず接続を切断する。</para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term xml:id="violently"><option>violently</option></term>

                <listitem>
                  <para>プライマリノードへの同期が許可され、ブロックデバイス上のデータがノードの 1 つに対して安定しているという前提に反す。<emphasis>危険なので使ってはならない。</emphasis></para>
                </listitem>
              </varlistentry>

              <varlistentry>
                <term xml:id="call-pri-lost"><option>call-pri-lost</option></term>

                <listitem>
                  <para>そのマシンがセカンダリに降格できる場合を除いて、いずれかのマシンの <option>pri-lost-after-sb</option> ヘルパープログラムを呼び出す。ヘルパープログラムはマシンを再起動することが期待され、ノードをセカンダリにする。どのマシンがヘルパープログラムを実行するかは、 <option>after-sb-0pri</option> ポリシーによって決定される。</para>
                </listitem>
              </varlistentry>
            </variablelist>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="data-integrity-alg"><option>data-integrity-alg </option> <replaceable>alg</replaceable></term>

          <listitem>
            <indexterm significance="normal">
              <primary>drbd.conf</primary>

              <secondary>data-integrity-alg</secondary>
            </indexterm>

            <para>ネットワーク経由で受け渡されるデータの整合性を担保するために、DRBD はハッシュ値を比較する機能を備えている。通常は、TCP/IP パケット自体のヘッダに含まれる 16 ビットチェックサムで保証される。</para>

            <para>このオプション値には、カーネルがサポートする任意のダイジェストアルゴリズムを指定できる。一般的なカーネルの場合、少なくとも <option>md5</option>, <option>sha1</option>, <option>crc32c</option> のどれかが利用できる。デフォルトでは、この機能は無効である。</para>

            <para>データ整合性に関する説明も参照のこと。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="tcp-cork"><option>tcp-cork</option></term>

          <listitem>
            <indexterm significance="normal">
              <primary>drbd.conf</primary>

              <secondary>tcp-cork</secondary>
            </indexterm>

            <para>DRBD は、 TCP ソケットの TCP_CORK オプションを使って、いつ追加データを受け取るか、あるいは送信キューのデータをいつフラッシュするかのヒントを得ている。この方法が悪影響を及ぼすネットワークスタックが少なくとも存在する。したがって、このオプションを導入された。 <option>tcp-cork</option> を <option>no</option> にセットすると、DRBD による TCP_CORK ソケットオプションの設定を無効にする。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="on-congestion"><option>on-congestion <replaceable>congestion_policy</replaceable></option></term>

          <term xml:id="congestion-fill"><option>congestion-fill <replaceable>fill_threshold</replaceable></option></term>

          <term xml:id="congestion-extents"><option>congestion-extents
          <replaceable>active_extents_threshold</replaceable></option></term>

          <listitem>
            <para>デフォルトでは、 TCP 送信キューが一杯になると、 DRBD は書き込みをブロックする。このことは DRBD が TCP で送信できるデータ量を越えてディスクに書きこもうとするアプリケーションの処理速度が低下することを意味する。</para>

            <para>DRBD-Proxy を使っている場合、送信キューが満杯になる直前に AHEAD/BEAIND モードに移行するのが望ましい。AHEAD/BEHIND モードでは、DRBD 間の通信は切断しないが、データレプリケーションは行われなくなる。</para>

            <para>AHEAD/BEHIND モードの利点は、たとえ DRBD-Proxy のバッファがすべての書き込み要求を受け入れるのに十分でなくても、アプリケーションが遅くならないことである。欠点は、対向ノードが後れをとっているため、同期状態に戻すために再同期が必要になることである。再同期中対向ノードのデータは不整合状態のままとなる。</para>

            <para><replaceable>congestion_policy</replaceable> では <option>block</option> と <option>pull-ahead</option> が使用できる。デフォルトは <option>block</option> である。<replaceable>Fill_threshold</replaceable> は 0 から 10GiB までの値を指定できる。デフォルト値は 0 で、これはチェックが無効になることを意味する。<replaceable>Active_extents_threshold</replaceable> は、 <option>al-extents</option> と同じ制限がある。</para>

            <para>AHEAD/BEHIND は DRBD8.3.10 以降で利用できる。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="wfc-timeout"><option>wfc-timeout <replaceable>time</replaceable></option></term>

          <listitem>
            <para>接続確立までの待機時間にタイムアウト値を設定する。<indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>wfc-timeout</secondary>
              </indexterm> init スクリプト <citerefentry>
                <refentrytitle>drbd</refentrytitle>

                <manvolnum>8</manvolnum> </citerefentry> は、 DRBD リソースの接続が確立するまで待ち続ける。後に起動されるクラスタ管理システムは、通常はリソース内のスプリットブレイン状態まではチェックしない。待ち時間を制限したい場合には、このパラメータ値を設定する。デフォルト値は 0 で、タイムアウトせずに待ち続ける。単位は秒である。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="degr-wfc-timeout"><option>degr-wfc-timeout <replaceable>time</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>degr-wfc-timeout</secondary>
              </indexterm>クラスタが縮退した場合には、コネクションタイムアウトを待つ。縮退とは、片方のノードしか動作していない状態を表す。このようなクラスタをリブートした場合には、対向ノードが一定時間内に起動する可能性が低いため、wfc-timeout ではなく degr-wfc-timeout が使われる。0 を指定すると、タイムアウトしなくなる。 </para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="outdated-wfc-timeout"><option>outdated-wfc-timeout <replaceable>time</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>outdated-wfc-timeout</secondary>
              </indexterm>相手ノードが無効になってから、接続確立までの待機時間にタイムアウト値を設定する。無効になった相手ノードが再起動されているデグレードクラスタ (1 つのノードしか残っていないクラスタ) では、このタイムアウト値は wfc-timeout の代わりに使用される。なぜなら、相手ノードはその間プライマリになることを許可されていないためである。0 を指定すると、タイムアウトしなくなる。 </para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="wait-after-sb"><option>wait-after-sb</option></term>

          <listitem>
            <para>このパラメータを指定すると、スプリットブレイン状態にあって接続自体が拒否されるような状態でも、起動スクリプトは接続まで待ち続けるようになる。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="become-primary-on"><option>become-primary-on <replaceable>node-name</replaceable></option></term>

          <listitem>
            <para>起動時にプライマリになるべきノードの名前を指定する。<replaceable>node-name</replaceable> はホスト名またはキーワードの <option>両方</option> かもしれない。このパラメータが指定されていない場合は、両ノードともセカンダリ状態で起動する。通常は、役割の決定をクラスタ管理システム (heartbeat など) に委ねることが多い。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="stacked-timeouts"><option>stacked-timeouts</option></term>

          <listitem>
            <para>スタックした上位デバイスについて、通常は <option>wfc-timeout</option> および <option>degr-wfc-timeout</option> は無視される。コネクションタイムアウト値には、代わりに <option>connect-int</option> の 2 倍の時間が使われる。<option>stacked-timeouts</option> を指定すると、DRBD は <option>wfc-timeout</option> および <option>degr-wfc-timeout</option> にもとづいて動作するようになる。スタックデバイスの対向ノードが多くの場合に利用できないケースや対向ノードがプライマリにならない場合に限って、このオプションを指定すべきである。誤用すれば、予期しないスプリットブレインが起きるリスクが生じる。 </para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="resync-rate"><option>resync-rate <replaceable>rate</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>resync-rate</secondary>
              </indexterm>DRBD の上位で動作するアプリケーションの円滑な実行のために、バックグラウンドの同期作業が利用する帯域幅を制限できる。デフォルト値は 250KB/秒、デフォルト単位は KB/秒である。K、 M、 G の接尾語を補って単位を変更できる。 </para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="use-rle"><option>use-rle</option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>use-rle</secondary>
              </indexterm>再同期開始時のハンドシェークの過程で各ノードのビットマップが交換され、ビット単位の OR 計算が行われる。これによって、どのブロックがダーティ (不一致) であるかについて、それぞれのノードは共通の認識を持つ。大容量デバイスではビットマップも大きくなり、帯域幅が小さいネットワークではその交換に時間を要する。</para>

            <para>典型的なビットマップは、すべてがセットされていない (クリーン) あるいはセットされている (ダーティ) いくつかのエリアに分かれている。このため、単純ではあるがランレングス符号化を採用することにより、ビットマップ交換のためのネットワークトラフィックを顕著に減らすことができる。</para>

            <para>過去のバージョンとの互換性のため、また高速ネットワークでは転送時間改善効果が少なく CPU 使用量が増えるため、デフォルトではこの機能は無効である。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="socket-check-timeout"><option>socket-check-timeout <replaceable>value</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>socket-check-timeout</secondary>
		</indexterm>DRBD-Proxy を使っていて大量のバッファを確保する必要がある環境では ping-timeout に非現実的な大きな値を指定しなければならないことがある。TCP コネクションが開始したときの安定するのを待つ局面でも、 DRBD はデフォルトで ping-timeout を使ってしまう。DRBD-Proxy は通常、同じデータセンターに配置されているため、長い待機時間は DRBD の接続プロセスを妨げる可能性がある。</para>
	    <para>このような場合、<option>socket-check-timeout</option> に DRBD と DRBD-Proxy 間の round trip time(RTT) を設定するとよい。たいていの場合 1 である。</para>
	    <para>デフォルトの単位は 10 分の 1 秒である。デフォルト値は 0 で socket-check-timeout 値の代わりに <option>ping-timeout</option> 値を使用する。8.4.5 から導入された。</para>

          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="resync-after"><option>resync-after <replaceable>res-name</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>resync-after</secondary>
              </indexterm>デフォルトでは全てのデバイスの再同期処理が並行して行われる。resync-after で依存性を定義すると、リソース <replaceable>res-name</replaceable> が connected 状態 (つまり再同期の完了後) になってからリソースの再同期を開始する。 </para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="al-extents"><option>al-extents <replaceable>extents</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>al-extents</secondary>
              </indexterm>DRBD はホットエリアを自動的に検出する。このパラメータを指定すると、ホットエリアの大きさを制御できる。各エクステントは、下位デバイスの 4MB の領域になる。予定外の事情によってプライマリノードがクラスタから切り離されると、そのときのホットエリアのデータは、次回接続したときの再同期の対象になる。このデータ構造は、メタデータ領域に書き込まれる。したがって、ホットエリアの状態更新は、メタデータデ バイスへの書き込みを引き起こす。エクステント値を大きくすると、再同期所要時間が長くなるが、メタデータの更新頻度を減らすことができる。 <replaceable>extents</replaceable> のデフォルト値は 1237 である。(最小値:7、最大値:65534)</para>
	    <para>有効な最大値はもっと小さくなる点に注意が必要であり、メタデータのデバイスの作成方法によっても異なる。次のマニュアルページを参照、<citerefentry><refentrytitle>drbdmeta</refentrytitle><manvolnum>8</manvolnum></citerefentry> を参照。有効な最大値は 919 *  (使用できる オンディスクのアクティビティログのリングバッファ領域 /4KB -1)  である。リングバッファはデフォルトで 32KB で、有効な最大値は 6433 である (データは 25GiB 以上カバーしている)。下位デバイスの量とレプリケーションリンク全体が 5 分以内で再同期できるようにすることを推奨する。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="al-updates"><option>al-updates <group choice="req" rep="norepeat">
	     <arg choice="plain" rep="norepeat">yes</arg>
	     <arg choice="plain" rep="norepeat">no</arg>
	  </group></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>
                <secondary>al-updates</secondary>
	      </indexterm>DRBD のアクティビティログ処理の書き込みによって、プライマリノードのクラッシュ後の部分的な (ビットマップに基づく) 再同期でノードを up-to-date に復帰させる事ができるようになる。<option>al-updates</option> を <option>no</option> に設定すると通常の運用パフォーマンスが向上するかもしれないが、クラッシュ時にプライマリが再接続した際にはフル同期となる。デフォルト値は <option>yes</option> である。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="verify-alg"><option>verify-alg <replaceable>hash-alg</replaceable></option></term>

          <listitem>
            <para><command moreinfo="none">verify</command> サブコマンドでディスク内容をオンライン照合する際、 DRBD はビット単位の比較ではなく、ブロックごとのハッシュ値を計算し、対向ノードのハッシュ値と比較する。照合に利用するハッシュアルゴリズムは、このパラメータで指定する。オプション値には、カーネルがサポートする任意のダイジェストアルゴリズムを指定できる。一般的なカーネルの場合、少なくとも <option>md5</option>, <option>sha1</option>, <option>crc32c</option> のどれかが利用できる。デフォルトでは、この機能は無効である。オンライン照合を有効にするには、このパラメータを明示的に設定する必要がある。 </para>

            <para>データ整合性に関する説明も参照のこと。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="csums-alg"><option>csums-alg <replaceable>hash-alg</replaceable></option></term>

          <listitem>
            <para>csums-alg が指定されていない場合、再同期プロセスはすべてのマークされたデータブロックをコピー元からコピー先に転送するこのオプションを指定すると、マークされたデータブロックのハッシュ値を最初に送り、ハッシュ値が一致しないブロックについてのみデータを転送する。</para>

            <para>帯域幅が小さい回線を使うとき、このオプションは有用である。クラッシュしたプライマリノードが復帰したとき、アクティビティログに記録されたすべてのブロックが再同期の対象となる。しかし大部分のブロックは同期が取れている。このため、 <option>csums-alg</option> を指定することによって、 CPU  の使用量と引き換えに必要な転送量を減らせる。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="c-plan-ahead"><option>c-plan-ahead <replaceable>plan_time</replaceable></option></term>

          <term xml:id="c-fill-target"><option>c-fill-target <replaceable>fill_target</replaceable></option></term>

          <term xml:id="c-delay-target"><option>c-delay-target <replaceable>delay_target</replaceable></option></term>

          <term xml:id="c-max-rate"><option>c-max-rate <replaceable>max_rate</replaceable></option></term>

          <listitem>
            <para><replaceable>plan_time</replaceable> に正の値を指定すると、再同期速度を動的に調整できるようになる。これは、 <replaceable>fill_target</replaceable> に指定した一定速度で、あるいは <replaceable>delay_target</replaceable> に指定した一定の遅延でデータを送信バッファに送り込むことによって実現される。調整における最大値は <replaceable>max_rate</replaceable> で指定する。</para>

            <para><replaceable>plan_time</replaceable> パラメータで調節機能の機敏さを設定する。大きな値を設定すると、調節機能のレスポンスが低下する。この値は最低でもネットワークの RTT の 5 倍以上を指定する。通常のデータ経路では <replaceable>fill_target</replaceable> に 4k から 100k を指定するのが適切である。DRBD-Proxy を使用する場合には、代わりに <replaceable>delay_target</replaceable> を使用するのが望ましい。<replaceable>delay_target</replaceable> は <replaceable>fill_target</replaceable> が 0 の場合にのみ使用できる。初期値は RTT の 5  倍が適切である。<replaceable>Max_rate</replaceable> には DRBD 間または DRBD-Proxy 間の帯域幅あるいはディスク帯域幅を指定する。</para>

            <para><replaceable>plan_time</replaceable> のデフォルト値は 0 で、0.1 秒単位で指定する。<replaceable>Fill_target</replaceable> のデフォルト値は 0 でセクタ数を指定する。<replaceable>Delay_target</replaceable> のデフォルト値は 1 (100 ミリ秒) で 0.1 秒単位で指定する。<replaceable>Max_rate</replaceable> のデフォルト値は 10240 (100MiB/s) で、 KiB/s 単位で指定する。</para>

            <para>動的な再同期速度の調整と設定は、DRBD 8.3.9 から使用できる。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="c-min-rate"><option>c-min-rate <replaceable>min_rate</replaceable></option></term>

          <listitem>
            <para>同期元のプライマリノードは、アプリケーションの書き込みと再同期の書き込みの配分を管理する必要がある。<replaceable>min_rate</replaceable> を指定すると、再同期のための帯域幅の最大値が min_rate になり、利用可能な帯域幅の残りの部分をアプリケーション I/O のために確保する。</para>

            <para>0 という値には特別な意味がある事に注意が必要である。0 は再同期速度の制限を完全になくすため、アプリケーションの速度を劇的に低下させる可能性がある。アプリケーションの速度低下を避けたい場合には、この値に 1 を指定する。</para>

            <para>注意:このパラメータ名は動的な調整速度の下限値のように見えるが、実際は異なる。DRBD-Proxy のバッファが満杯のとき、動的調整機能は <option>c-min-rate</option> の設定から独立して、再同期速度を 0 まで下げることができる。</para>

            <para><replaceable>Min_rate</replaceable> のデフォルト値は 4096(4MiB/s) であり、 KiB/s 単位で指定する。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="on-no-data-accessible"><option>on-no-data-accessible
          <replaceable>ond-policy</replaceable></option></term>

          <listitem>
            <para>この設定は、ディスクレスモードが発生した際の操作を指定する。設定できるポリシーは <option>io-error</option> と <option>suspend-io</option> である。</para>

            <para><replaceable>ond-policy</replaceable> に <option>suspend-io</option> を設定すると、最後に接続していたデータストレージから、もしくは、 <command moreinfo="none">drbdadm resume-io <replaceable>res</replaceable></command> コマンドにより、 I/O を再開することができる。後者はもちろん I/O エラーを発生させる。</para>

            <para>デフォルトは <option>io-error</option> である。この設定は、 DRBD 8.3.9 から有効である。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="cpu-mask"><option>cpu-mask <replaceable>cpu-mask</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>cpu-mask</secondary>
              </indexterm>DRBD のカーネルスレッドに CPU アフィニティマスクを設定する。<replaceable>cpu-mask</replaceable> のデフォルト値は 0 で、 DRBD のカーネルスレッドがマシン全体の CPU にまたがって動作することを意味している。この値は 16 進表現で指定する必要がある。値が大きすぎると切り捨てられる。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="pri-on-incon-degr"><option>pri-on-incon-degr <replaceable>cmd</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>pri-on-incon-degr</secondary>
              </indexterm>このパラメータで指定するハンドラは、ノードがプライマリで、縮退 (片方のノードしか動作していない状態) し、ローカルのコピーデータに不整合がある場合に呼び出される。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="pri-lost-after-sb"><option>pri-lost-after-sb <replaceable>cmd</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>pri-lost-after-sb</secondary>
              </indexterm>このパラメータで指定するハンドラはノードが現在プライマリで、スプリットブレイン後の自動回復プロセスが失敗したときに呼び出される。その結果、このノードのデータが放棄されるようにする。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="pri-lost"><option>pri-lost <replaceable>cmd</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>pri-lost</secondary>
              </indexterm>このパラメータに指定したハンドラは、ノードが現在プライマリであるにもかかわらず、 DRBD のアルゴリズムが同期先だと判断した場合に実行される。このような状態になった場合、このノードはプライマリであることをやめるべきである。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="fence-peer"><option>fence-peer <replaceable>cmd</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>fence-peer</secondary>
              </indexterm>このパラメータは<option>フェンシング</option>メカニズムの一部を構成する。このパラメータに指定したハンドラは、対向ノードを無効状態にする必要が生じたときに実行される。呼び出されたコマンドは、 DRBD が使っている通信経路とは別の経路を使うべきである。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="local-io-error"><option>local-io-error <replaceable>cmd</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>local-io-error</secondary>
              </indexterm>このパラメータに指定したハンドラは DRBD がローカルの I/O サブシステムから I/O エラーを受けた時に実行される。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="initial-split-brain"><option>initial-split-brain <replaceable>cmd</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>initial-split-brain</secondary>
              </indexterm>DRBD がスプリットブレインを検出した場合に使用される。このハンドラはスプリットブレインが発生した場合に警告を通知する。問題が解決した場合でも通知する。 </para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="split-brain"><option>split-brain <replaceable>cmd</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>split-brain</secondary>
              </indexterm>このパラメータに指定したハンドラは、スプリットブレイン状態が検出され、回復できない場合に呼び出される。修復のための手作業が必要なので、このハンドラは、誰かにこのことを通知するのが望ましい。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="before-resync-target"><option>before-resync-target <replaceable>cmd</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>before-resync-target</secondary>
              </indexterm>このパラメータで指定したハンドラは、同期先で再同期が開始される直前に呼び出される。下位ブロックデバイスのスナップショットを取得する、などの用途が考えられる。</para>
          </listitem>
        </varlistentry>

        <varlistentry>
          <term xml:id="after-resync-target"><option>after-resync-target <replaceable>cmd</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>after-resync-target</secondary>
              </indexterm>このパラメータで指定したハンドラは、不整合状態だったノードで、ディスクの再同期が完了した直後に呼び出される。<option>before-resync-target</option> ハンドラで作成したスナップショットを削除する、などの用途が考えられる。</para>
          </listitem>
        </varlistentry>
      </variablelist>
    </refsect2>

    <refsect2>
      <title>Other Keywords</title>

      <variablelist>
        <varlistentry>
          <term xml:id="include"><option>include <replaceable>file-pattern</replaceable></option></term>

          <listitem>
            <para><indexterm significance="normal">
                <primary>drbd.conf</primary>

                <secondary>include</secondary>
              </indexterm><replaceable>file-pattern</replaceable> に指定したワイルドカード形式に合致する全てのファイルを組み込む。<option>include</option> ステートメントはトップレベルでのみ使用でき、セクション内では使用できない。</para>
          </listitem>
        </varlistentry>
      </variablelist>
    </refsect2>
  </refsect1>

  <refsect1 id="data-integrity">
    <title>Notes on data integrity</title>

    <para>DRBD がミラーしたデータの整合性を保証する方法は 2 つあり、これらはそれぞれ別々のものである。オンライン照合と <option>network</option> セクションの <option>data-integrity-alg</option> である。</para>

    <para>どちらのメカニズムの場合でも、データの転送中に DRBD の上位プログラムがディスクに書き込みを行った場合、不整合ではないのに不整合と判断されることがある。それはスワップの発生、グローバル同期中の付加、ワークロードの打ち切りや書き換えであるかもしれず、必ずしもデータ整合性の問題をもたらさない。通常イニシエータがデータ転送を行う際には、データブロックがディスク上のデータ構造の一部でない事を認識しているか、すぐに正確なデータで再実行される。</para>

    <para><option>data-integrity-alg</option> は、&quot;Digest integrity check FAILED: Ns |x\n&quot; というエラーを受信側のログに書きだす。 N は相殺されたセクタのオフセットで、x はバイト単位のリクエストサイズである。それから接続を切り、再接続して、迅速に再同期をする。同時に送信側が変更を検知した場合、&quot;Digest mismatch, buffer modified by upper layers during write: Ns +x\n&quot; という警告がでるが、これは誤検出である。変更されていないデータが tcp バッファにコピーされると、送信側はこれらのバッファの変化をすぐに検出するかもしれない。この場合、受信側はそれに気づかない。</para>

    <para>直近の例 (2007 年) では系統的なデータ損傷のケースがあり、ギガビット NIC の TCP/IP オフロードエンジンとドライバが原因だった。メインメモリから NIC への DMA データ転送時にデータ破壊が起きていた。TCP チェックサムは NIC 側で計算されるため、この種のデータエラーは オンライン照合 <option>verify</option> または <option>data-integrity-alg</option> を使わない限り検出できない。</para>

    <para><option>data-integrity-alg</option> は CPU 負荷が大きいため、テスト期間中のみ使うことを推奨する。その後は、たとえば月に 1 回程度、負荷が低い時間帯にオンライン照合を実施するのが望ましい。</para>
  </refsect1>

  <refsect1>
    <title>Version</title>

    <simpara>このドキュメントは DRBD バージョン 8.4.0 向けに改訂されている。</simpara>
  </refsect1>

  <refsect1>
    <title>Author</title>

    <simpara>Written by Philipp Reisner <email>philipp.reisner@linbit.com</email> and Lars
    Ellenberg <email>lars.ellenberg@linbit.com</email>.</simpara>
  </refsect1>

  <refsect1>
    <title>Reporting Bugs</title>

    <simpara>Report bugs to <email>drbd-user@lists.linbit.com</email>.</simpara>
  </refsect1>

  <refsect1>
    <title>Copyright</title>

    <simpara>Copyright 2001-2008 LINBIT Information Technologies, Philipp Reisner, Lars Ellenberg.
    This is free software; see the source for copying conditions. There is NO warranty; not even
    for MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.</simpara>
  </refsect1>

  <refsect1>
    <title>See Also</title>

    <para>
      <citerefentry><refentrytitle>drbd</refentrytitle><manvolnum>8</manvolnum></citerefentry>,
      <citerefentry><refentrytitle>drbddisk</refentrytitle><manvolnum>8</manvolnum></citerefentry>,
      <citerefentry><refentrytitle>drbdsetup</refentrytitle><manvolnum>8</manvolnum></citerefentry>,
      <citerefentry><refentrytitle>drbdmeta</refentrytitle><manvolnum>8</manvolnum></citerefentry>,
      <citerefentry><refentrytitle>drbdadm</refentrytitle><manvolnum>8</manvolnum></citerefentry>,
      <ulink url="http://www.drbd.org/users-guide/"><citetitle>DRBD User's Guide</citetitle></ulink>,
      <ulink url="http://www.drbd.org/"><citetitle>DRBD web site</citetitle></ulink>
    </para>

  </refsect1>
</refentry>
